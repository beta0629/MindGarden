# 어드민 대시보드 V2 — 기간별 통계 시각화 그룹 스펙 (v2 갱신)

**작성**: core-designer  
**일자**: 2026-07-28  
**목적**: 어드민 대시보드 V2 「시각화 그룹」의 시인성(고대비) 개선, 「진행」 우선순위 하향, 증감/목표대비 KPI 강화.  
**기준**: `PENCIL_DESIGN_GUIDE.md`, B0KlA 디자인 시스템, `unified-design-tokens.css`

---

## 1. 화면 와이어프레임 (ASCII)

### 1.1 데스크톱 (1280px 이상)
목표 달성률 및 전기간 대비 증감을 좌상단 KPI 존에 먼저 노출하여 핵심 성과를 즉시 스캔할 수 있도록 레이아웃을 변경합니다.

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ ▌ 시각화 그룹 (Visualization Group)             [ 일간 | 주간 | 월간 | 년간 ] │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ V6/V6b. 요약 KPI 및 목표 달성 (최상단 배치로 가독성 극대화)             │ │
│ │                                                                         │ │
│ │ [예약 건수]          [완료 건수]           [목표 달성률 (완료 기준)]    │ │
│ │  120건  [▲ 12%]      85건  [▼ 5%]          85% 달성 (목표 100건)        │ │
│ │  지난달 107건        지난달 89건           [■■■■■■■■□□]           │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────┐ ┌───────────────────────────────────┐ │
│ │ V2. 상담 현황 (스택/그룹 바)      │ │ V3. 추이 비교 (멀티라인)          │ │
│ │ [예약] [완료]   (진행: 약화)      │ │ [예약] [완료]   (진행: 약화)      │ │
│ │                                   │ │                                   │ │
│ │   █   █   █   █   █               │ │    /\      /\                     │ │
│ │   █   █   █   █   █               │ │   /  \    /  \                    │ │
│ │   █   █   █   █   █               │ │  /    \/\/    \                   │ │
│ │  Jan Feb Mar Apr May              │ │ Jan Feb Mar Apr May               │ │
│ └───────────────────────────────────┘ └───────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 모바일 (375px ~ 767px)
1열 스택 구조 유지, 증감 KPI 최상단 노출.

```text
┌───────────────────────────────────┐
│ ▌ 시각화 그룹                     │
│ [일간|주간|월간|년간] (가로스크롤)│
├───────────────────────────────────┤
│ ┌───────────────────────────────┐ │
│ │ V6/V6b. KPI 및 목표 달성률    │ │
│ │ 예약: 120건 [▲ 12%]           │ │
│ │ 완료:  85건 [▼  5%]           │ │
│ │ 목표: 85% 달성 (100건)        │ │
│ └───────────────────────────────┘ │
├───────────────────────────────────┤
│ ┌───────────────────────────────┐ │
│ │ V2. 상담 현황 (스택/그룹 바)  │ │
│ └───────────────────────────────┘ │
├───────────────────────────────────┤
│ ┌───────────────────────────────┐ │
│ │ V3. 추이 비교 (멀티라인)      │ │
│ └───────────────────────────────┘ │
└───────────────────────────────────┘
```

---

## 2. 시각화 시리즈 색상 토큰 (진한 그린 복원 + 축·배경 대비)

v2 고대비 팔레트(블루/에메랄드)는 톤이 어색해 **기존 B0KlA/P0 진한 올리브 그린**을 메인으로 되돌립니다.  
가독성은 시리즈 색을 바꾸지 않고 **배경·축·범례 명도 차**로만 보강합니다. (Hex 하드코딩 금지)

| 시리즈 | 색상 토큰 | 설명 / 적용 기준 |
|--------|-----------|------------------|
| **예약** (Booked) | `var(--mg-color-secondary-main)` | 대기/준비. 차분한 secondary. 완료(진한 그린)과 명도로 구분. |
| **완료** (Completed) | `var(--mg-color-primary-main)` | 긍정/달성. 브랜드 진한 올리브 그린 (`#3D5246` 계열). |
| **진행** (In Progress) | `var(--ad-b0kla-text-secondary)` | 2순위 지표. 시선을 뺏지 않도록 무채색 계열로 약화. |

### 2.1 대비 보강 (시리즈 외)
- **축 tick / 범례**: `var(--mg-v2-color-text-primary)` — 차트 배경 대비 강화
- **그리드**: `var(--mg-v2-color-border-default)` — border-light 대비 한 단계 상향
- **차트 카드 배경**: `var(--mg-color-background-main)` (그룹 surface와 명도 대비 유지)
- **비활성 pill**: `var(--mg-color-surface-main)` / 활성: `var(--mg-color-primary-main)`

### 2.2 「진행」(IN_PROGRESS) 시리즈 처리 결정 및 범례 규칙
- **기본 비노출(off) 혹은 2순위 약화**: 볼륨은 예약과 완료 중심으로 표현합니다. 차트에서 1급 데이터로 보이지 않게 하며, 렌더링 시에는 약화된 색상(`var(--ad-b0kla-text-secondary)`)을 사용합니다.
- **범례 (Legend)**: 범례 위치를 가장 후순위로 내리고, 시각적 우선순위가 가장 낮은 스타일 톤을 적용합니다.

---

## 3. MoM / 전기간 증감 배지 UX (V6)

직전 기간(지난달, 전주 등) 대비 증감률을 직관적으로 보여주는 배지 스펙입니다.
- **토큰 재사용**: 기존 아토믹 배지 토큰/밝은 배경 대비 토큰을 활용합니다 (`--mg-badge-status-*`).

| 상태 | 아이콘/형태 | 배지 배경 토큰 (`bg`) | 텍스트/아이콘 토큰 (`text`) |
|------|-------------|-----------------------|-----------------------------|
| **상승 (긍정)** | `▲ 12%` | `var(--mg-badge-status-success-bg)` | `var(--mg-badge-status-success-text)` |
| **하락 (부정)** | `▼ 5%` | `var(--mg-badge-status-danger-bg)` | `var(--mg-badge-status-danger-text)` |
| **보합/동일** | `- 0%` | `var(--mg-badge-status-neutral-bg)` | `var(--mg-badge-status-neutral-text)` |
| **Empty** | 데이터 없음 | (배지 미노출) | "이전 데이터 없음" 툴팁 처리 혹은 빈칸 유지 |

- *참고: 예약/완료 건수 상승은 어드민 측면에서 기본적으로 긍정(Success)으로 간주합니다.*

---

## 4. 목표 vs 실적 KPI/배지 UX (V6b)

기간별 목표 대비 실적(주로 완료 기준)을 보여주는 요소. 관리자가 목표 달성 여부를 스캔하는 핵심 영역.

- **달성률 표시 텍스트**: `var(--ad-b0kla-title-color)` (24px, 600)
- **달성 상태 배지**:
  - **초과 달성 (>= 100%)**: `var(--mg-badge-status-success-bg)` 배경 + `var(--mg-badge-status-success-text)` 텍스트 (예: "목표 달성")
  - **진행 중/미달 (< 100%)**: `var(--mg-badge-status-info-bg)` 배경 + `var(--mg-badge-status-info-text)` 텍스트 (예: "85% 진행")
- **프로그레스 바**: 트랙 배경 `var(--ad-b0kla-bg)` 위에 채움(fill) `var(--mg-color-primary-main)` (100% 초과 시에도 바가 뚫고 나가지 않도록 100% 한정 처리).

---

## 5. 코더 핸드오프 섹션 (구현 가이드)

본 스펙은 디자이너 시안/UX 스펙으로, 코드 파일은 수정되지 않았습니다. 프론트엔드/백엔드 코더는 다음을 참조해 구현합니다.

### 5.1 데이터 필드 제안 (BE/FE)
- **이전 기간 (Previous Period)**: `previousPeriodBooked`, `previousPeriodCompleted` (또는 FE가 배열 인덱스 차분으로 계산).
- **증감률 (Growth Rate)**: `growthRateBooked`, `growthRateCompleted` (%) 계산.
- **목표 (Target)**: API에 목표치(`targetCompleted` 등)가 없다면 FE 설정값(Placeholder 상수)이라도 두어 표현하거나, 인벤토리 후 확정.

### 5.2 구현 우선순위
1. **최상단 KPI 컴포넌트 신설/배치**: V6(증감 배지) + V6b(목표대비 KPI)를 시각화 그룹 상단에 가장 먼저 읽히게 배치.
2. **차트 시리즈 및 색상**: 예약(`var(--mg-color-secondary-main)`), 완료(`var(--mg-color-primary-main)` 진한 그린) 중심. 진행은 차트에서 약화(`var(--ad-b0kla-text-secondary)`) 또는 제외. 대비는 축/배경/범례 명도로만 보강.
3. **증감 배지 로직**: 이전 기간 데이터와 비교해 상승/하락/보합 상태 판별 및 해당 CSS 변수 적용.

### 5.3 주의 (하드코딩 및 게이트 제약)
- 차트 및 UI 요소의 **모든 색상은 CSS 변수(`var(--...)`)**만을 사용해야 합니다. hex(`#XXXXXX`) 텍스트 하드코딩은 절대 금지(게이트 D11).
- `AdminDashboardB0KlA.css` 및 `unified-design-tokens.css`의 토큰 체계를 존중합니다.
- 안전한 표시: 0으로 나누기, NaN 방지를 위해 `toSafeNumber` 또는 `safeDisplay` 사용 필수.
