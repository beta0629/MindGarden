/**
 * StatusBadge - common 컴포넌트 re-export (호환용)
 * @deprecated common/StatusBadge 직접 import 권장. 사용처는 components/common/StatusBadge 로 변경.
 */
export { default } from '../../../../common/StatusBadge';
