[E2 본문 시작]

# 회기 부여/차감 및 ERP 거래 분리 가능성 분석 (E2)

> **목적**: 통합 스케줄링 페이지의 「신규 매칭」 버튼(Dj / `integrated-schedule__btn-new-mapping`) 부터 시작되는 단일 경로(선납 → 매칭)를 7 영역으로 인벤토리하고, **옵션 B (예약 우선 → 사후 결제)** 도입 시 각 영역에서 발생할 GAP 을 식별한다. **읽기 전용 — 코드 0건 수정.**

(E2 본문 전체는 본 conversation 의 직전 system_notification — task_id `45cb027f-1f4b-4831-8191-656694b30fc4` — `response` 필드 ```` ```markdown ... ``` ```` 안에 있는 본문 전체를 그대로 사용. §1~§6 + 부록 A 포함.)

[E2 본문 끝]