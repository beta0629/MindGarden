# Trinity 프론트엔드 환경 변수 예시
# Trinity Frontend Environment Variables Example
#
# 사용 방법:
# 1. 이 파일을 복사하여 .env.local 파일을 만듭니다
# 2. 실제 값으로 채워넣습니다
# 3. .env.local 파일은 Git에 커밋되지 않습니다 (보안)
#
# macOS/Linux: cp env.local.example .env.local
# Windows: copy env.local.example .env.local

# ============================================
# API 설정
# ============================================
# 백엔드 API 기본 URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# ============================================
# 토스페이먼츠 설정
# ============================================
# 토스페이먼츠 클라이언트 키 (프론트엔드 전용)
# 테스트 키 (현재 설정됨)
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_jExPeJWYVQ56w5kKdmpqV49R5gvN

# 운영 키 (상용 오픈 전에 제공 예정)
# NEXT_PUBLIC_TOSS_CLIENT_KEY=live_ck_...

# 토스페이먼츠 테스트 모드
# 테스트 환경: true
# 운영 환경: false (상용 오픈 전에 변경)
NEXT_PUBLIC_TOSS_TEST_MODE=true

