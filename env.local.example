# 로컬 개발 환경 변수 설정 예시
# Local Development Environment Variables Example
#
# 사용 방법:
# 1. 이 파일을 복사하여 .env.local 파일을 만듭니다
# 2. 실제 값으로 채워넣습니다
# 3. .env.local 파일은 Git에 커밋되지 않습니다 (보안)
#
# macOS/Linux: cp env.local.example .env.local
# Windows: copy env.local.example .env.local

# ============================================
# 데이터베이스 설정 (개발 서버 DB)
# ============================================
# 개발 서버 DB 호스트 주소 (예: beta74.cafe24.com 또는 IP 주소)
DB_HOST=your-dev-db-host

# 데이터베이스 포트
DB_PORT=3306

# 데이터베이스 이름
DB_NAME=core_solution

# 데이터베이스 사용자명
DB_USERNAME=mindgarden_dev

# 데이터베이스 비밀번호 (필수: 실제 비밀번호로 변경)
DB_PASSWORD=MindGardenDev2025!@#

# ============================================
# JWT 설정
# ============================================
JWT_SECRET=dev-jwt-secret-key-change-me-to-secure-value

# ============================================
# 개인정보 암호화 설정
# ============================================
PERSONAL_DATA_ENCRYPTION_KEY=dev-encryption-key-32-chars-long
PERSONAL_DATA_ENCRYPTION_IV=dev-iv-16-chars

# ============================================
# 서버 설정
# ============================================
SERVER_PORT=8080
FRONTEND_BASE_URL=http://localhost:3000

# ============================================
# 기타 설정 (선택사항)
# ============================================
# SMS 인증 (로컬에서는 보통 비활성화)
SMS_AUTH_ENABLED=false

# OpenAI API (필요한 경우만)
OPENAI_API_KEY=

# ============================================
# 결제 시스템 설정 (토스페이먼츠)
# ============================================
# 토스페이먼츠 시크릿 키 (백엔드 전용, 웹훅 검증 등에 사용)
# 테스트 키 (현재 설정됨)
PAYMENT_TOSS_SECRET_KEY=test_sk_ORzdMaqN3w59ZLadepPbr5AkYXQG

# 운영 키 (상용 오픈 전에 제공 예정)
# PAYMENT_TOSS_SECRET_KEY=live_sk_...

# 토스페이먼츠 보안 키 (웹훅 검증용)
# 테스트 키 (현재 설정됨)
PAYMENT_TOSS_WEBHOOK_SECRET=09fb9726652b997b8b7f4fe44782cd08f8cda6bb8ed8af243ebe9248d967ba2c

# 운영 키 (상용 오픈 전에 제공 예정)
# PAYMENT_TOSS_WEBHOOK_SECRET=...

# 토스페이먼츠 상점 아이디 (MID, Merchant ID)
# 토스페이먼츠에서 발급하는 상점 아이디입니다. 최대 길이는 14자입니다.
# 테스트 환경에서는 선택사항일 수 있으나, 운영 환경에서는 필수입니다.
# PAYMENT_TOSS_MERCHANT_ID=your-merchant-id

# 토스페이먼츠 API 기본 URL (기본값: https://api.tosspayments.com)
PAYMENT_TOSS_BASE_URL=https://api.tosspayments.com

# 토스페이먼츠 시뮬레이션 모드
# 테스트 환경: true
# 운영 환경: false (상용 오픈 전에 변경)
PAYMENT_TOSS_SIMULATION_MODE=true

# ============================================
# 프론트엔드 환경 변수 (Trinity)
# ============================================
# 참고: frontend-trinity/.env.local 파일도 별도로 생성해야 합니다
# 참고: You also need to create frontend-trinity/.env.local file separately
#
# frontend-trinity/env.local.example 파일을 참고하여
# frontend-trinity/.env.local 파일을 생성하세요.
#
# See frontend-trinity/env.local.example for reference
# and create frontend-trinity/.env.local file.


