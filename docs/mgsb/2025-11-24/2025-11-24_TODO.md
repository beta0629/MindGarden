# 2025-11-24 작업 계획 및 TODO 리스트

**작성일**: 2025-11-24  
**목적**: 온보딩 플로우 완성 및 역할 템플릿/대시보드 생성 문제 해결  
**최종 업데이트**: 2025-11-24 (오후)  
**다음 작업 가이드**: `docs/mgsb/2025-11-24/2025-11-24_NEXT_STEPS.md` 참조  
**진행 상황**: 주요 작업 10개 완료 (대시보드 위젯 편집 UI, 온보딩 로그인 기능 추가, 온보딩 대시보드 위젯 편집, 학원 정산 시스템 기본 구조 완료)

---

## 📊 진행 상황 요약

- ✅ 역할 템플릿 생성 문제 해결
- ✅ 역할 할당 문제 해결
- ✅ 프론트엔드 로그인 테스트
- ✅ Ops 로그인 및 권한 문제 해결
- ✅ 대시보드 상세 페이지 권한 문제 해결
- ✅ 테넌트 생성 시 카테고리 정보 저장 구현
- ✅ 대시보드 위젯 편집 UI 개발 (CSS 상수화 완료)
- ✅ 온보딩 로그인 기능 추가 (기존 요청 조회 및 이어하기)
- ✅ 온보딩 대시보드 설정 UI 개발 (템플릿 선택, 위젯 편집 간편 버전, API 연동, 백엔드 적용 완료)
- ✅ 학원 컴포넌트 기본 구조 확인 (프론트엔드/백엔드/DB 스키마)
- ✅ 학원 정산 시스템 기본 구조 완료 (데이터베이스, 엔티티, Repository, Service, Controller)

**상세 진행사항**: `docs/mgsb/2025-11-24/2025-11-24_PROGRESS_SUMMARY.md` 참조

---

## 🎯 주요 목표

1. **역할 템플릿 생성 문제 해결** ⭐⭐⭐
2. **역할 할당 문제 해결** ⭐⭐⭐
3. **Ops 로그인 및 권한 문제 해결** ⭐⭐⭐
4. **대시보드 상세 페이지 권한 문제 해결** ⭐⭐⭐
5. **온보딩 대시보드 설정 UI 개발** ⭐⭐⭐
6. **대시보드 위젯 편집 UI 개발** ⭐⭐⭐
7. **학원 컴포넌트 개발** ⭐⭐⭐
8. **대시보드 생성 문제 해결** ⭐⭐
9. **프론트엔드 로그인 테스트** ⭐⭐

---

## 📋 TODO 리스트

### 1. 역할 템플릿 생성 문제 해결 (우선순위: 높음) ✅ 완료

#### 1.1 ProcessOnboardingApproval 프로시저 확인
- [x] `ApplyDefaultRoleTemplates` 프로시저가 호출되는지 확인
- [x] 프로시저 실행 순서 확인
- [x] 프로시저 실행 로그 확인

#### 1.2 역할 템플릿 매핑 데이터 확인
- [x] `role_template_mappings` 테이블에 CONSULTATION 업종 매핑 확인
- [x] `role_templates` 테이블에 기본 템플릿 존재 확인
- [x] 매핑 데이터가 없으면 생성

#### 1.3 ApplyDefaultRoleTemplates 프로시저 디버깅
- [x] 프로시저 직접 실행 테스트
- [x] 오류 메시지 확인
- [x] 프로시저 로직 검증

**완료 시간**: 2025-11-24 오전

---

### 2. 역할 할당 문제 해결 (우선순위: 높음) ✅ 완료

#### 2.1 역할 템플릿 생성 후 역할 할당
- [x] `OnboardingServiceImpl.assignAdminRoleToUser()` 메서드 확인
- [x] "원장" 역할 이름 검색 로직 확인
- [x] 역할 할당 로직 수정 (PL/SQL 프로시저로 이동)

#### 2.2 역할 할당 테스트
- [x] 온보딩 승인 후 역할 할당 확인
- [x] `user_role_assignments` 테이블에 데이터 생성 확인
- [x] 역할 기반 권한 체크 테스트

**완료 시간**: 2025-11-24 오전  
**주요 변경사항**: `CreateTenantAdminAccount` 프로시저에 역할 할당 로직 추가

---

### 3. Ops 로그인 및 권한 문제 해결 (우선순위: 높음) ✅ 완료

#### 3.1 Ops Portal 로그인 문제 확인
- [x] Ops Portal 로그인 API 확인
- [x] 세션 생성 확인
- [x] 인증 필터 동작 확인
- [x] 로그인 실패 원인 파악

#### 3.2 Ops Portal 권한 문제 확인
- [x] Ops Portal 사용자 권한 확인
- [x] 권한 체크 로직 확인
- [x] 접근 제어 필터 확인
- [x] 권한 부족 오류 메시지 확인

#### 3.3 권한 문제 해결
- [x] 권한 체크 로직 수정 (필요시)
- [x] 접근 제어 필터 수정 (필요시)
- [x] Ops Portal 사용자 권한 설정 확인
- [x] 권한 테스트

**완료 시간**: 2025-11-24 오후  
**테스트 결과**: ✅ 모든 API 접근 성공

---

### 4. 대시보드 상세 페이지 권한 문제 해결 (우선순위: 높음) ✅ 완료

#### 4.1 대시보드 상세 페이지 접근 문제 확인
- [x] 대시보드 상세 페이지 API 확인
- [x] 권한 체크 로직 확인
- [x] 접근 제어 필터 확인
- [x] 권한 부족 오류 메시지 확인

#### 4.2 대시보드 권한 체크 로직 확인
- [x] 대시보드 소유권 확인 로직
- [x] 테넌트 접근 권한 확인 로직
- [x] 역할 기반 접근 권한 확인 로직
- [x] 권한 체크 실패 원인 파악

#### 4.3 권한 문제 해결
- [x] 대시보드 권한 체크 로직 수정 (TenantContextHolder에서 세션 User 정보로 폴백)
- [x] 접근 제어 필터 수정 (UserRepository 주입하여 DB에서 최신 정보 조회)
- [x] 권한 테스트
- [x] 대시보드 상세 페이지 접근 확인 (배포 대기 중)

**완료 시간**: 2025-11-24 오후  
**주요 변경사항**: `TenantDashboardController`에서 세션 User 정보로 tenantId 추출 로직 추가

---

### 5. 대시보드 위젯 편집 UI 개발 (우선순위: 높음)

#### 5.1 현재 상태 확인
- [x] 현재 상태 확인 완료
  - [x] `DashboardFormModal.js`에서 JSON 직접 입력 방식 확인
  - [x] 관리자가 위젯을 시각적으로 편집할 수 없음
  - [x] 드래그 앤 드롭 기능 없음 (계획만 있음)
  - [x] `react-sortablejs` 라이브러리 이미 설치됨
  - [x] 화면 위치: 코어 솔루션 테넌트 관리자 페이지 (`/admin/dashboards`)

#### 5.2 위젯 편집 UI 컴포넌트 개발 ✅ 완료
- [x] **위젯 편집기 컴포넌트 생성**
  - [x] `DashboardWidgetEditor.js` 컴포넌트 생성
    - 위치: `frontend/src/components/admin/DashboardWidgetEditor.js`
  - [x] 위젯 목록 표시 (사용 가능한 위젯 타입 목록)
  - [x] 위젯 추가 버튼
  - [x] 위젯 삭제 기능
  - [x] 위젯 설정 모달

- [x] **드래그 앤 드롭 레이아웃 편집기**
  - [x] 라이브러리 확인: `react-sortablejs` 이미 설치됨
  - [x] `DashboardLayoutEditor.js` 컴포넌트 생성
    - 위치: `frontend/src/components/admin/DashboardLayoutEditor.js`
  - [x] `react-sortablejs` 사용하여 드래그 앤 드롭 구현
  - [x] 위젯 위치 자동 저장 (position 정보)
  - [x] CSS 클래스 기반 그리드 시스템 구현 (인라인 스타일 제거)

- [x] **위젯 설정 UI**
  - [x] `WidgetConfigModal.js` 컴포넌트 생성
  - [x] 위젯 타입 표시 (읽기 전용)
  - [x] 위젯별 설정 폼 (config 필드 - JSON 편집)
  - [x] 위치 설정 (row, col, span)
  - [x] 표시 조건 설정 (visibility - JSON 편집)

#### 5.3 DashboardFormModal 통합 ✅ 완료
- [x] `DashboardFormModal.js`에 위젯 편집기 통합
  - 위치: `frontend/src/components/admin/DashboardFormModal.js`
  - 경로: `/admin/dashboards` (코어 솔루션 테넌트 관리자 페이지)
- [x] JSON 직접 입력 방식과 시각적 편집 방식 탭 추가
- [x] 위젯 편집 결과를 JSON으로 자동 변환
- [x] JSON 검증 및 오류 표시
- [x] CSS와 비즈니스 로직 분리
- [x] CSS 상수화 및 인라인/하드코딩 제거

#### 5.4 위젯 편집 UI 테스트 ⏳ 대기 (우선순위 1)
- [ ] 위젯 추가 테스트
- [ ] 위젯 삭제 테스트
- [ ] 위젯 위치 변경 테스트
- [ ] 위젯 설정 변경 테스트
- [ ] 대시보드 저장 및 로드 테스트
- **다음 작업**: 브라우저에서 `/admin/dashboards` 접속하여 테스트 진행

**예상 시간**: 3-5일 (드래그 앤 드롭 포함), 1-2일 (기본 편집 UI만)

**참고 문서**:
- `WIDGET_CONFIGURATION_GUIDE.md`
- `META_SYSTEM_DASHBOARD_SCHEMA.md`
- `WIDGET_ARCHITECTURE.md`

**필요 라이브러리**:
- ✅ `react-sortablejs` (이미 설치됨) - 기본 드래그 앤 드롭
- ✅ `sortablejs` (이미 설치됨)
- 선택적: `react-grid-layout` (고급 기능 필요 시 추가 설치)

**화면 위치**:
- 코어 솔루션 테넌트 관리자 페이지
- 경로: `/admin/dashboards`
- 파일: `frontend/src/components/admin/DashboardFormModal.js`

---

### 6. 온보딩 로그인 기능 추가 (우선순위: 높음)

#### 6.1 온보딩 시작 전 로그인 옵션
- [ ] `OnboardingLogin.tsx` 컴포넌트 생성
  - 파일: `frontend-trinity/components/onboarding/OnboardingLogin.tsx`
- [ ] 온보딩 페이지 진입 시 로그인 상태 확인
  - 파일: `frontend-trinity/app/onboarding/page.tsx`
- [ ] 로그인 옵션 UI 구현
  - "로그인하고 시작하기" 버튼
  - "로그인 없이 시작하기" 버튼
- [ ] 기존 온보딩 요청 조회 기능
  - 로그인한 사용자의 온보딩 요청 목록 조회
  - 상태 표시 및 선택 옵션

#### 6.2 온보딩 진행 상태 저장/복원
- [ ] 온보딩 진행 상태 저장 기능
  - 각 단계 완료 시 진행 상태 저장
  - 로그인하지 않은 경우 localStorage 임시 저장
  - 로그인한 경우 서버에 저장
- [ ] 진행 상태 복원 기능
  - 로그인 후 진행 상태 자동 복원
  - 마지막 진행 단계부터 이어서 진행

#### 6.3 온보딩 상태 확인 및 수정
- [ ] 온보딩 상태 확인 UI 확장
  - 파일: `frontend-trinity/app/onboarding/status/page.tsx`
  - 로그인한 사용자의 온보딩 요청 자동 조회
  - 상태별 표시 (PENDING, APPROVED, REJECTED, ON_HOLD)
- [ ] 온보딩 요청 수정 기능
  - PENDING 상태인 온보딩 요청만 수정 가능
  - 온보딩 플로우로 돌아가서 수정
  - 기존 입력값 자동 채우기
- [ ] 승인 완료 시 자동 이동
  - "테넌트 관리 페이지로 이동" 버튼
  - 클릭 시 테넌트 관리 페이지로 자동 이동

**예상 시간**: 7일

**참고 문서**:
- `ONBOARDING_LOGIN_FEATURE_PLAN.md`
- `ONBOARDING_VS_CORE_SOLUTION_ROLES.md`

---

### 7. 온보딩 결제 및 PG 설정 확장 (우선순위: 높음)

#### 6.1 Step 4 결제 화면 확장
- [ ] `Step4Payment.tsx` 확장
  - 파일: `frontend-trinity/components/onboarding/Step4Payment.tsx`
- [ ] PG 사 선택 UI
  - 드롭다운 또는 카드 형태
  - 지원 PG: TOSS, IAMPORT, KAKAO, NAVER, PAYPAL, STRIPE 등
- [ ] PG 설정 정보 입력
  - API Key 입력
  - Secret Key 입력 (암호화)
  - Merchant ID 입력
  - 기타 PG별 필수 정보
- [ ] PG 설정 관리
  - 등록된 PG 설정 목록 표시
  - PG 설정 변경
  - PG 설정 삭제
  - 기본 PG 설정 선택
- [ ] 결제 수단 관리 확장
  - 결제 수단 변경 (기존 등록 기능 확장)
  - 결제 수단 삭제
- [ ] Step 4 활성화
  - 파일: `frontend-trinity/app/onboarding/page.tsx`
  - `false && step === 4` → `step === 4`로 변경

**예상 시간**: 3-5일

**참고 문서**:
- `ONBOARDING_VS_CORE_SOLUTION_ROLES.md`
- `ONBOARDING_REGISTRATION_PLAN.md`

---

### 8. 마이페이지 테넌트 관리 기능 추가 (우선순위: 높음)

#### 8.1 마이페이지에 테넌트 관리 탭 추가 및 추가 인증
- [ ] `TenantManagementSection.js` 컴포넌트 생성
  - 파일: `frontend/src/components/mypage/components/TenantManagementSection.js`
- [ ] 마이페이지에 "테넌트 관리" 탭 추가
  - 파일: `frontend/src/components/mypage/MyPage.js`
- [ ] 테넌트 관리자 여부 확인 로직
  - 테넌트 관리자만 탭 표시
- [ ] `TenantManagementVerificationModal.js` 컴포넌트 생성
  - 파일: `frontend/src/components/mypage/components/TenantManagementVerificationModal.js`
- [ ] 추가 인증 기능 구현
  - 이메일 인증 코드 발송/검증
  - 휴대폰 인증 코드 발송/검증
  - 인증 방법 선택 (이메일/휴대폰)
- [ ] 인증 상태 관리
  - 인증 성공 시 30분 유지
  - 세션 스토리지에 인증 상태 저장
  - 인증 만료 시 재인증 요구
- [ ] 인증 실패 처리
  - 최대 5회 시도 제한
  - 시도 횟수 초과 시 잠시 후 재시도 안내

#### 8.2 결제 관리 기능
- [ ] `PaymentManagement.js` 컴포넌트 생성
  - 파일: `frontend/src/components/mypage/components/PaymentManagement.js`
- [ ] 결제 수단 목록 표시
- [ ] 결제 수단 추가/변경/삭제
- [ ] PG 사 설정 관리
  - PG 사 목록 표시
  - PG 사 추가/변경/삭제
- [ ] 구독 정보 표시
  - 현재 요금제 표시
  - 요금제 변경 기능

#### 8.3 위젯 설정 기능
- [ ] `WidgetManagement.js` 컴포넌트 생성
  - 파일: `frontend/src/components/mypage/components/WidgetManagement.js`
- [ ] 대시보드 위젯 목록 표시
- [ ] 위젯 추가/제거
- [ ] 드래그 앤 드롭 레이아웃 편집
- [ ] 위젯 설정 저장

#### 8.4 와일드카드 도메인 설정 기능
- [ ] `DomainManagement.js` 컴포넌트 생성
  - 파일: `frontend/src/components/mypage/components/DomainManagement.js`
- [ ] 와일드카드 도메인 입력 UI
- [ ] 도메인 유효성 검증
  - 도메인 형식 검증
  - 중복 도메인 체크
- [ ] 서브도메인 미리보기
  - {tenant}.도메인 형태로 표시
- [ ] DNS 설정 안내
  - CNAME 레코드 설정 안내
- [ ] 온보딩에 와일드카드 도메인 입력 추가
  - 파일: `frontend-trinity/components/onboarding/Step1BasicInfo.tsx` 또는 새 스텝

#### 8.5 백엔드 API
- [ ] 테넌트 정보 조회 API
  - `GET /api/tenant/info`
- [ ] 와일드카드 도메인 설정 API
  - `PUT /api/tenant/domain`
- [ ] 도메인 중복 체크 API
  - `GET /api/tenant/domain/check`
- [ ] 결제/PG/위젯 관리 API
  - 기존 API 활용 또는 확장

**예상 시간**: 12일 (추가 인증 포함)

**참고 문서**:
- `MYPAGE_TENANT_MANAGEMENT_PLAN.md`
- `ONBOARDING_VS_CORE_SOLUTION_ROLES.md`

---

### 9. 온보딩 대시보드 설정 UI 개발 (우선순위: 높음)

#### 6.1 온보딩 플로우에 대시보드 설정 스텝 추가 ✅ 완료
- [x] `Step6DashboardSetup.tsx` 컴포넌트 생성
  - 위치: `frontend-trinity/components/onboarding/Step6DashboardSetup.tsx`
- [x] 온보딩 플로우에 스텝 추가
  - 파일: `frontend-trinity/app/onboarding/page.tsx`
- [x] 기본 레이아웃 및 스타일

#### 6.2 템플릿 기반 위젯 선택 UI ✅ 완료
- [x] 업종별/역할별 템플릿 데이터 정의
  - 상담소 템플릿 (관리자, 상담사, 내담자)
  - 학원 템플릿 (관리자, 선생님, 학생)
- [x] 템플릿 카드 컴포넌트
- [x] 템플릿 선택 애니메이션
  - 호버 시 확대 효과 (CSS transition)
  - 선택 시 펄스 애니메이션
  - 체크 표시 애니메이션

#### 6.3 위젯 편집 UI (간편 버전) ✅ 완료
- [x] 위젯 목록 표시
  - 현재 위젯 목록 태그 형태로 표시
  - 위젯 삭제 버튼 포함
- [x] 위젯 추가/제거 기능
  - 사용 가능한 위젯 목록에서 추가
  - 현재 위젯 목록에서 삭제
  - 편집 모드 진입/종료
- [x] 위젯 편집 UI 스타일
  - 위젯 태그 스타일
  - 편집 버튼 및 액션 버튼
  - CSS 상수화 및 인라인 스타일 제거
- [ ] 위젯 추가/삭제 애니메이션 (향후 개선)
  - 추가: 슬라이드 인 애니메이션
  - 삭제: 페이드 아웃 애니메이션
- [ ] 실시간 미리보기 업데이트 (향후 개선)
  - 페이드 전환 애니메이션

#### 6.4 안내 메시지 및 가이드
- [ ] 단계별 안내 메시지 컴포넌트
  - 시작 안내
  - 템플릿 선택 안내
  - 위젯 추가 안내
  - 완료 안내
- [ ] 안내 메시지 애니메이션
  - 페이드 인/아웃
  - 슬라이드 인
- [ ] 툴팁 및 도움말
  - 각 위젯 설명 툴팁
  - "이 위젯이 무엇인가요?" 버튼

#### 6.5 애니메이션 효과 구현
- [ ] 페이지 전환 애니메이션 (슬라이드/페이드)
- [ ] 템플릿 카드 애니메이션 (호버, 선택, 펄스)
- [ ] 위젯 추가/삭제 애니메이션 (슬라이드, 페이드)
- [ ] 미리보기 업데이트 애니메이션 (페이드 전환)
- [ ] 진행 상태 애니메이션 (프로그레스 바, 체크 표시)

#### 6.6 API 연동 및 저장 ✅ 완료
- [x] 대시보드 설정 저장 API 연동
  - 선택된 템플릿을 `checklistJson`에 저장
  - 편집된 위젯 목록을 `checklistJson`의 `dashboardWidgets`에 저장
- [x] 온보딩 완료 후 대시보드 설정 적용
  - 백엔드에서 `checklistJson`의 `dashboardTemplates` 읽어서 대시보드 생성 시 적용
  - 백엔드에서 `checklistJson`의 `dashboardWidgets` 읽어서 대시보드 생성 시 우선 적용
  - 템플릿 ID 기반 위젯 설정 생성
  - 위젯 목록 기반 대시보드 설정 생성 (`createDashboardConfigFromWidgets` 메서드)
- [x] 위젯 편집 간편 버전 구현
  - 템플릿 선택 후 "위젯 편집" 버튼으로 편집 모드 진입
  - 위젯 추가/삭제 기능
  - 현재 위젯 목록 표시 및 관리
  - 사용 가능한 위젯 목록에서 추가
- [ ] 건너뛰기 옵션 (나중에 설정 가능)

**예상 시간**: 5일 (템플릿 + 위젯 편집 + 애니메이션 + 안내 메시지)

**참고 문서**:
- `ONBOARDING_DASHBOARD_SETUP_PLAN.md`
- `DASHBOARD_WIDGET_EDITOR_PLAN.md`
- `WIDGET_CONFIGURATION_GUIDE.md`

**애니메이션 라이브러리**:
- 권장: CSS 애니메이션 (가벼움, 성능 우수)
- 선택적: Framer Motion (고급 애니메이션 필요 시)

**화면 위치**:
- 온보딩 플로우 (Trinity 홈페이지)
- 경로: `/onboarding` (Step 5)
- 파일: `frontend-trinity/components/onboarding/Step5DashboardSetup.tsx`

**참고**: 온보딩에서 초기 설정을 완료하고, 이후 코어 솔루션에서 컨텐츠 위주로 운영하며 필요 시 설정 변경

---

### 10. 코어 솔루션 역할 재정의 (우선순위: 중간)

#### 8.1 코어 솔루션 설정 메뉴 재구성
- [ ] 설정 메뉴 최소화
  - 컨텐츠 중심으로 재구성
  - 자주 사용하는 설정만 노출
  - 고급 설정은 숨김 처리
- [ ] 대시보드 컨텐츠 강화
  - 컨텐츠 중심 위젯 우선 표시
  - 설정 관련 위젯은 최소화
- [ ] 설정 기능 정리
  - 대시보드 위젯 편집 (고급, 필요 시에만)
  - 결제 및 PG 관리 (변경/삭제)
  - 요금제 관리 (변경)
  - 기본 정보 수정 (제한적)

**예상 시간**: 2-3일

**참고 문서**:
- `ONBOARDING_VS_CORE_SOLUTION_ROLES.md`

---

### 9. 학원 컴포넌트 개발 (우선순위: 높음)

#### 10.1 학원 컴포넌트 기본 구조 확인 ✅ 완료
- [x] 현재 구현된 학원 컴포넌트 확인
  - [x] `AcademyDashboard.js` 확인 (강좌/반/수강 등록 관리 통합 화면)
  - [x] `CourseList.js`, `CourseForm.js` 확인 (강좌 관리)
  - [x] `ClassList.js`, `ClassForm.js` 확인 (반 관리)
  - [x] `EnrollmentList.js`, `EnrollmentForm.js` 확인 (수강 등록 관리)
- [x] 백엔드 API 엔드포인트 확인
  - [x] `AcademyCourseController` (`/api/v1/academy/courses`)
  - [x] `AcademyClassController` (`/api/v1/academy/classes`)
  - [x] `AcademyEnrollmentController` (`/api/v1/academy/enrollments`)
  - [x] `AcademyAttendanceController` (`/api/v1/academy/attendances`)
  - [x] `AcademyRegistrationController` (`/api/v1/academy/registration`)
- [x] 데이터베이스 스키마 확인 (courses, classes, enrollments)
  - [x] `Course` 엔티티 확인 (강좌 정보)
  - [x] `Class` 엔티티 확인 (반 정보)
  - [x] `ClassEnrollment` 엔티티 확인 (수강 등록)
  - [x] `Attendance` 엔티티 확인 (출결)

#### 9.2 학원 핵심 기능 구현 (5가지 흐름)
- [x] **1. 신규생 상담 및 등록** ✅ 완료
  - [x] 상담 예약 API 구현
  - [x] 상담 기록 관리
  - [x] 수강 등록 프로세스
- [x] **2. 반(Class)/강좌(Course) 배정 및 수강 관리** ✅ 완료
  - [x] 강좌 관리 API 완성 (이미 구현됨)
  - [x] 반 관리 API 완성 (이미 구현됨)
  - [x] 수강생 배정 기능 (수강 등록으로 처리)
  - [x] 시간표 설정 기능
- [x] **3. 출결 관리 및 학부모 알림** ✅ 완료
  - [x] 출결 기록 API (이미 구현됨)
  - [x] 출결 통계
  - [x] 학부모 알림 발송 기능
- [x] **4. 월별 수강료 청구 및 결제** ✅ 완료
  - [x] 청구 스케줄 관리 (CRUD + 실행 로직 완료)
  - [x] 청구서 생성 및 발송 (생성, 발행, 발송, 취소 완료)
  - [x] 결제 연동 (생성, 완료, 취소, 환불 완료)
  - [x] 영수증 발급 (영수증 번호 자동 생성 포함)
  - [x] 배치 작업 (월별 청구서 자동 생성, 연체 업데이트)
- [x] **5. 정산(수강료/강사/본사) 자동화** ✅ 기본 구조 완료 (고도화 필요)
  - [x] 정산 배치 프로세스 (월별 정산 자동 계산 완료)
  - [x] 매출/수수료 계산 (결제 완료 금액 기준 계산 완료)
  - [x] 강사 급여 계산 (기본 비율 계산 완료, **수업 횟수 기반 계산 고도화 필요** ⏳)
  - [x] HQ 정산 리포트 (정산 항목 조회 완료)
  - [x] 데이터베이스 스키마 생성 (academy_settlements, academy_settlement_items)
  - [x] 엔티티 클래스 생성 (AcademySettlement, AcademySettlementItem)
  - [x] Repository 인터페이스 생성
  - [x] Service 인터페이스 및 구현체 생성
  - [x] Controller 생성
  - [ ] **고도화 작업** (우선순위 2)
    - [ ] 강사별 정산 계산 로직 고도화 (수업 횟수, 수강료 비율 기반)
    - [ ] 정산 항목 상세 생성 (강사별, 반별, 강좌별)

#### 9.3 학원 위젯 구현 ⏳ 대기 (우선순위 2)
- [ ] **학원 일정 위젯** (`AcademyScheduleWidget`)
  - [ ] 수업 일정 조회
  - [ ] 시간표 표시
  - [ ] 일정 등록/수정/삭제
- [ ] **출석 관리 위젯** (`AcademyAttendanceWidget`)
  - [ ] 출석 체크
  - [ ] 출석 통계
  - [ ] 결석/지각 관리
- [ ] **학원 통계 위젯** (`AcademyStatsWidget`)
  - [ ] 수강생 수 통계
  - [ ] 수업 진행률 통계
  - [ ] 출석률 통계
- [ ] **수강생 관리 위젯** (`AcademyStudentWidget`)
  - [ ] 수강생 목록
  - [ ] 수강생 상세 정보
  - [ ] 수강 등록/취소
- **다음 작업**: 우선순위 1 작업 완료 후 진행

#### 9.4 학원 컴포넌트 활성화 설정
- [ ] 온보딩 시 학원 컴포넌트 자동 활성화 (business_type=ACADEMY)
- [ ] 컴포넌트 활성화 상태 확인
- [ ] 학원 전용 메뉴/라우팅 설정

**예상 시간**: 2-3주 (전체), 1-2일 (MVP 최소 기능)

**참고 문서**: 
- `PHASE1_ACADEMY_MVP_PLAN.md`
- `PHASE1_FLOW_AND_SCREEN_DESIGN.md`
- `PHASE1_API_SPEC.md`

---

### 10. 대시보드 생성 문제 해결 (우선순위: 중간) ✅ 완료

#### 10.1 역할 템플릿 기반 대시보드 생성
- [x] `TenantDashboardServiceImpl.createDefaultDashboards()` 메서드 확인
- [x] 역할 템플릿 검색 로직 확인
- [x] 대시보드 생성 로직 확인 (정상 작동)

#### 10.2 기본 대시보드 생성 테스트
- [x] 온보딩 승인 후 대시보드 생성 확인
- [x] `tenant_dashboards` 테이블에 데이터 생성 확인
- [x] 기본 대시보드 4개 생성 확인 (역할별)

**완료 시간**: 2025-11-24 오후  
**테스트 결과**: ✅ 대시보드 자동 생성 정상 작동

---

### 11. 프론트엔드 로그인 테스트 (우선순위: 중간) ✅ 완료

#### 11.1 테넌트 관리자 로그인 테스트
- [x] `test-category1763957121@example.com` / `Test1234!@#` 로그인 테스트
- [x] 세션 생성 확인
- [x] 사용자 정보 조회 확인

#### 11.2 대시보드 표시 확인
- [x] 대시보드 목록 조회 API 테스트
- [x] 기본 대시보드 표시 확인
- [x] 대시보드 라우팅 확인

#### 11.3 기능 접근 권한 확인
- [x] 역할 기반 권한 체크 확인
- [x] 메뉴 접근 권한 확인
- [x] API 접근 권한 확인

**완료 시간**: 2025-11-24 오후  
**테스트 결과**: ✅ 모든 항목 성공

---

### 12. 추가 작업 (우선순위: 낮음)

#### 12.1 온보딩 플로우 문서화
- [ ] 온보딩 플로우 다이어그램 작성
- [ ] 프로시저 실행 순서 문서화
- [ ] 문제 해결 과정 문서화

#### 12.2 테스트 데이터 정리
- [ ] 테스트 테넌트 정리 (필요시)
- [ ] 테스트 사용자 정리 (필요시)
- [ ] 테스트 온보딩 요청 정리 (필요시)

**예상 시간**: 30분-1시간

---

## 🔍 디버깅 체크리스트

### ProcessOnboardingApproval 프로시저 확인
```sql
-- 프로시저 존재 확인
SHOW PROCEDURE STATUS WHERE Name = 'ProcessOnboardingApproval';

-- 프로시저 정의 확인
SHOW CREATE PROCEDURE ProcessOnboardingApproval;

-- ApplyDefaultRoleTemplates 호출 확인
-- (프로시저 내부 코드 확인)
```

### 역할 템플릿 매핑 확인
```sql
-- 역할 템플릿 매핑 확인
SELECT * FROM role_template_mappings 
WHERE business_type = 'CONSULTATION';

-- 역할 템플릿 확인
SELECT * FROM role_templates 
WHERE business_type = 'CONSULTATION' OR business_type IS NULL;
```

### 테넌트 역할 확인
```sql
-- 테넌트 역할 확인
SELECT * FROM tenant_roles 
WHERE tenant_id = 'test-tenant-1763912519';
```

### 역할 할당 확인
```sql
-- 역할 할당 확인
SELECT ura.*, u.email, r.role_name
FROM user_role_assignments ura
JOIN users u ON ura.user_id = u.id
JOIN tenant_roles r ON ura.tenant_role_id = r.tenant_role_id
WHERE u.tenant_id = 'test-tenant-1763912519';
```

### 대시보드 확인
```sql
-- 대시보드 확인
SELECT * FROM tenant_dashboards 
WHERE tenant_id = 'test-tenant-1763912519';
```

### Ops Portal 권한 확인
```sql
-- Ops Portal 사용자 확인
SELECT * FROM users 
WHERE email LIKE '%ops%' OR email LIKE '%admin%' OR role = 'BRANCH_SUPER_ADMIN';

-- Ops Portal 권한 확인
SELECT u.*, r.role_name, r.role_code
FROM users u
LEFT JOIN user_role_assignments ura ON u.id = ura.user_id
LEFT JOIN tenant_roles r ON ura.tenant_role_id = r.tenant_role_id
WHERE u.email = 'superadmin@mindgarden.com';
```

### 대시보드 상세 페이지 권한 확인
```sql
-- 대시보드 소유권 확인
SELECT 
    td.id,
    td.tenant_id,
    td.name,
    td.created_by,
    u.email as creator_email
FROM tenant_dashboards td
LEFT JOIN users u ON td.created_by = u.email
WHERE td.id = ?;

-- 사용자 대시보드 접근 권한 확인
SELECT 
    u.id as user_id,
    u.email,
    u.tenant_id,
    td.id as dashboard_id,
    td.name as dashboard_name
FROM users u
JOIN tenant_dashboards td ON u.tenant_id = td.tenant_id
WHERE u.id = ? AND td.id = ?;
```

---

## 📊 예상 작업 시간

| 작업 | 예상 시간 | 실제 시간 | 우선순위 | 상태 |
|------|----------|----------|---------|------|
| 역할 템플릿 생성 문제 해결 | 1-2시간 | 1시간 | 높음 | ✅ 완료 |
| 역할 할당 문제 해결 | 1시간 | 1시간 | 높음 | ✅ 완료 |
| Ops 로그인 및 권한 문제 해결 | 1-2시간 | 1시간 | 높음 | ✅ 완료 |
| 대시보드 상세 페이지 권한 문제 해결 | 1-2시간 | 1시간 | 높음 | ✅ 완료 |
| 프론트엔드 로그인 테스트 | 1-2시간 | 30분 | 중간 | ✅ 완료 |
| 테넌트 생성 시 카테고리 정보 저장 | - | 1시간 | 높음 | ✅ 완료 |
| 온보딩 로그인 기능 추가 | 7일 (로그인 옵션 + 진행 상태 저장/복원 + 상태 확인/수정) | 높음 |
| 마이페이지 테넌트 관리 기능 추가 | 12일 (결제 + 위젯 + 와일드카드 도메인 + 추가 인증) | 높음 |
| 온보딩 결제 및 PG 설정 확장 | 3-5일 (PG 사 등록/변경/삭제) | 높음 |
| 온보딩 대시보드 설정 UI 개발 | 5일 (템플릿 + 위젯 편집 + 애니메이션) | 높음 |
| 코어 솔루션 역할 재정의 | 2-3일 (설정 메뉴 재구성) | 중간 |
| 대시보드 위젯 편집 UI 개발 | 3-5일 (드래그 앤 드롭), 1-2일 (기본) | 높음 |
| 학원 컴포넌트 개발 | 2-3주 (전체), 1-2일 (MVP) | 높음 |
| 대시보드 생성 문제 해결 | 1-2시간 | 30분 | 중간 | ✅ 완료 |
| 프론트엔드 로그인 테스트 | 1-2시간 | 30분 | 중간 | ✅ 완료 |
| 추가 작업 | 30분-1시간 | - | 낮음 | ⏳ 대기 |
| **총 예상 시간** | **7-12시간 + 학원 컴포넌트** | **약 6시간** | | **6개 완료** |

---

## 🎯 성공 기준

1. ✅ 온보딩 승인 후 역할 템플릿 자동 생성
2. ✅ 관리자 역할 자동 할당
3. ✅ Ops Portal 로그인 성공
4. ✅ Ops Portal 권한 체크 통과
5. ✅ 대시보드 상세 페이지 접근 성공
6. ✅ 온보딩 로그인 기능 추가 완료 (로그인 옵션 + 진행 상태 저장/복원)
7. ✅ 온보딩 상태 확인 및 수정 기능 완료
8. ✅ 마이페이지 테넌트 관리 기능 추가 완료 (결제 + 위젯 + 와일드카드 도메인)
9. ✅ 와일드카드 도메인 설정 기능 완료 (마이페이지 + 온보딩)
10. ✅ 온보딩 결제 및 PG 설정 확장 완료 (PG 사 등록/변경/삭제)
11. ✅ 온보딩 대시보드 설정 UI 완성 (템플릿 선택 + 위젯 편집 간편 버전 + 애니메이션)
12. ✅ 온보딩 시 안내 메시지와 함께 간편하게 대시보드 설정 가능
13. ✅ 학원 정산 시스템 기본 구조 완료 (데이터베이스, 엔티티, Repository, Service, Controller)
14. ✅ 온보딩 대시보드 설정 백엔드 연동 완료 (dashboardWidgets 처리)
13. ✅ 학원 정산 시스템 기본 구조 완료 (데이터베이스, 엔티티, Repository, Service, Controller)
14. ✅ 온보딩 대시보드 설정 백엔드 연동 완료 (dashboardWidgets 처리)
13. ✅ 코어 솔루션 역할 재정의 완료 (컨텐츠 중심)
14. ✅ 대시보드 위젯 편집 UI 완성 (드래그 앤 드롭 포함)
15. ✅ 관리자가 시각적으로 위젯 추가/삭제/배치 가능
16. ✅ 학원 컴포넌트 기본 기능 구현 완료
17. ✅ 학원 위젯 4개 구현 완료
8. ✅ 기본 대시보드 4개 자동 생성
9. ✅ 프론트엔드 로그인 성공
10. ✅ 대시보드 표시 확인
11. ✅ 기능 접근 권한 확인
12. ✅ 테넌트 생성 시 카테고리 정보 자동 저장
13. ✅ 카테고리별 테넌트 조회 가능

---

## 📝 참고 자료

- [2025-11-23 작업 요약](./2025-11-23_WORK_SUMMARY.md)
- [온보딩 프로세스 문서](../ONBOARDING_ADMIN_ACCOUNT_PROCESS.md)
- [대시보드 관리 시스템](../TENANT_DASHBOARD_MANAGEMENT_SYSTEM.md)
- [ProcessOnboardingApproval 프로시저 코드](../CORE_SOLUTION_PLSQL_ARCHITECTURE.md)

---

---

## 🚀 다음 작업 가이드

**즉시 시작 가능한 작업 (우선순위 1):**
1. 위젯 편집 UI 테스트 (`/admin/dashboards` 접속하여 테스트)
2. 온보딩 대시보드 설정 UI 테스트 (`/onboarding` 접속하여 Step 6 테스트)
3. 학원 정산 시스템 테스트 (API 테스트)

**상세 가이드**: `docs/mgsb/2025-11-24/2025-11-24_NEXT_STEPS.md` 참조

---

**작성자**: 개발팀  
**상태**: 주요 작업 완료, 테스트 단계  
**시작 예정 시간**: 2025-11-24 오전  
**최종 업데이트**: 2025-11-24 (오후)

