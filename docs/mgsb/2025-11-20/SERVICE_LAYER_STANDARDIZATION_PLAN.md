# 서비스 레이어 표준화 계획

**작성일**: 2025-11-20  
**버전**: 1.0.0  
**상태**: 계획 수립 중

---

## 📋 개요

CoreSolution 플랫폼의 서비스 레이어를 표준화하여 일관성 있는 아키텍처를 구축하고, 유지보수성과 테스트 용이성을 향상시킵니다.

---

## 🔍 현재 상태 분석

### 서비스 레이어 구조

1. **인터페이스와 구현체 분리**
   - 일부 서비스는 인터페이스와 구현체가 분리되어 있음
   - 일부 서비스는 구현체만 존재 (인터페이스 없음)

2. **기존 Base 서비스**
   - `BaseService<T, ID>` (consultation 패키지)
   - `BaseServiceImpl<T, ID>` (consultation 패키지)
   - `BaseTenantService<T, ID, REQ, RES>` (core 패키지)
   - `BaseTenantServiceImpl<T, ID, REQ, RES>` (core 패키지)

3. **서비스 개수**
   - 약 100개 이상의 서비스 존재
   - 인터페이스: 약 80개
   - 구현체: 약 100개

### 문제점

1. **인터페이스 누락**
   - 일부 서비스는 구현체만 존재하여 테스트와 모킹이 어려움
   - 의존성 주입 시 인터페이스 대신 구현체를 직접 참조하는 경우 존재

2. **네이밍 불일치**
   - 대부분 `*Service` 인터페이스와 `*ServiceImpl` 구현체로 일관성 있음
   - 일부 예외 존재 가능성

3. **공통 로직 중복**
   - CRUD 로직이 여러 서비스에 중복 구현
   - Base 서비스를 활용하지 않는 서비스 존재

4. **트랜잭션 관리 불일치**
   - 일부 서비스는 클래스 레벨 `@Transactional` 사용
   - 일부 서비스는 메서드 레벨 `@Transactional` 사용
   - 일부 서비스는 트랜잭션 어노테이션 누락

5. **예외 처리 불일치**
   - 일부 서비스는 커스텀 예외 사용
   - 일부 서비스는 표준 예외 사용
   - 예외 처리 패턴이 일관되지 않음

---

## 🎯 표준화 목표

1. **인터페이스 필수화**
   - 모든 서비스는 인터페이스와 구현체 분리
   - 인터페이스가 없는 서비스에 인터페이스 추가

2. **일관된 네이밍**
   - 인터페이스: `*Service`
   - 구현체: `*ServiceImpl`

3. **Base 서비스 활용**
   - 공통 CRUD 로직은 Base 서비스 활용
   - 테넌트 기반 서비스는 `BaseTenantService` 활용

4. **트랜잭션 표준화**
   - 클래스 레벨 `@Transactional` 사용 (읽기 전용 메서드는 `@Transactional(readOnly = true)`)
   - 메서드 레벨 오버라이드 가능

5. **예외 처리 표준화**
   - 비즈니스 예외는 커스텀 예외 사용
   - 예외는 `GlobalExceptionHandler`에서 처리

---

## 📝 표준화 계획

### Phase 5.0: 분석 및 계획 수립 ✅ (진행 중)

**작업**:
1. 서비스 레이어 전체 조사
   - 인터페이스 존재 여부 확인
   - 구현체 존재 여부 확인
   - Base 서비스 활용 여부 확인
2. 문제점 식별
   - 인터페이스 누락 서비스 목록
   - 네이밍 불일치 서비스 목록
   - Base 서비스 미활용 서비스 목록
3. 우선순위 결정
   - 핵심 서비스 우선
   - 자주 사용되는 서비스 우선
4. 표준화 계획 문서 작성

**예상 시간**: 1-2시간

---

### Phase 5.1: 인터페이스 추가 (우선순위: P1) ✅ 완료 (2025-11-20)

**작업**:
1. ✅ 인터페이스가 없는 서비스 식별
   - 대부분의 서비스가 이미 인터페이스를 보유하고 있음을 확인
   - 검증된 서비스: SchemaService, ErdGenerationService, OnboardingApprovalService, AutoApprovalService, CacheService, BranchCommonCodeService, CodeInitializationService, PgConnectionTestService, TenantPgConfigurationService, ErdMetadataService, MermaidErdService, ErdChangeNotificationService, SchemaChangeErdRegenerationService 등
2. ✅ 인터페이스 존재 여부 확인
   - 약 126개 구현체 중 대부분이 인터페이스를 보유
   - 인터페이스가 없는 서비스는 매우 적음 (추가 조사 필요)
3. ⏳ 남은 작업
   - 인터페이스가 없는 서비스 최종 확인 및 인터페이스 추가 (필요시)

**완료 시간**: 약 30분 (조사 완료)

**결과**: 대부분의 서비스가 이미 인터페이스를 보유하고 있어 추가 작업이 거의 필요 없음

---

### Phase 5.2: Base 서비스 활용 (우선순위: P1) 🚧 분석 완료

**작업**:
1. ✅ Base 서비스 미활용 서비스 식별
   - Base 서비스 활용 현황 분석 문서 작성 (`BASE_SERVICE_USAGE_ANALYSIS.md`)
   - BaseTenantEntityServiceImpl 사용: 8개 서비스
   - Base 서비스 미사용: 약 100개 이상 서비스
2. ⏳ Base 서비스 상속으로 리팩토링 (우선순위별 진행)
   - 우선순위 1: UserServiceImpl (BaseService 활용 가능)
   - 우선순위 2: 엔티티 기반 서비스들 (BaseTenantEntityServiceImpl 활용)
   - 우선순위 3: 부분적 활용 가능한 서비스들
3. ⏳ 테스트 및 검증

**대상 서비스**:
- 우선순위 1: UserServiceImpl (1개)
- 우선순위 2: 엔티티 기반 서비스들 (추가 조사 필요)
- 우선순위 3: 부분적 활용 가능한 서비스들 (추가 조사 필요)

**예상 시간**: 3-4일 (우선순위별 점진적 진행)

**참고**: 복잡한 비즈니스 로직을 가진 서비스(AdminService, ErpService, StatisticsService 등)는 Base 서비스 활용이 어려울 수 있음

---

### Phase 5.3: 트랜잭션 표준화 (우선순위: P2) ✅ 분석 완료

**작업**:
1. ✅ 트랜잭션 어노테이션 사용 현황 조사
   - @Transactional 사용: 363개 매치 (72개 파일)
   - @Transactional(readOnly = true) 사용: 258개 매치 (37개 파일)
   - 클래스 레벨 @Transactional: 20개 파일
2. ✅ 트랜잭션 표준화 가이드 작성
   - `TRANSACTION_STANDARDIZATION_GUIDE.md` 작성
   - 표준화 규칙 및 패턴 정의
   - 마이그레이션 가이드 포함
3. ⏳ 표준 패턴 적용 (점진적 진행)
   - 클래스 레벨 `@Transactional` 확인
   - 읽기 전용 메서드 `@Transactional(readOnly = true)` 추가
   - 트랜잭션 전파 설정 확인

**완료 시간**: 분석 및 가이드 작성 약 1시간

**결과**: 대부분의 서비스가 클래스 레벨 `@Transactional`을 사용하고 있음. 읽기 전용 메서드에 `@Transactional(readOnly = true)` 추가가 주요 개선 사항.

---

### Phase 5.4: 예외 처리 표준화 (우선순위: P2) ✅ 분석 완료

**작업**:
1. ✅ 예외 처리 패턴 조사
   - throw new 사용: 314개 매치 (51개 파일)
   - throw new *Exception 사용: 20개 파일
   - 커스텀 예외 클래스: 3개 (EntityNotFoundException, ValidationException, ConnectionTestException)
2. ✅ 예외 처리 표준화 가이드 작성
   - `EXCEPTION_HANDLING_STANDARDIZATION_GUIDE.md` 작성
   - 표준화 규칙 및 패턴 정의
   - 마이그레이션 가이드 포함
3. ✅ 현재 상태 분석
   - GlobalExceptionHandler 잘 구현됨 ✅
   - 커스텀 예외 클래스 존재 ✅
   - 예외 처리 패턴 일관성 있음 ✅

**완료 시간**: 분석 및 가이드 작성 약 1시간

**결과**: 예외 처리 구조가 잘 구성되어 있음. 예외 메시지 개선 및 예외 유형 확장이 주요 개선 사항.

---

### Phase 5.5: 문서화 및 가이드 작성 (우선순위: P3) ✅ 완료 (2025-11-20)

**작업**:
1. ✅ 서비스 레이어 가이드 작성
   - `SERVICE_LAYER_GUIDE.md` 작성
   - 인터페이스 작성 가이드
   - 구현체 작성 가이드
   - Base 서비스 활용 가이드
   - 트랜잭션 관리 가이드
   - 예외 처리 가이드
   - 로깅 패턴 가이드
2. ✅ 코드 리뷰 체크리스트 작성
   - `SERVICE_LAYER_CODE_REVIEW_CHECKLIST.md` 작성
   - 서비스 인터페이스 체크리스트
   - 서비스 구현체 체크리스트
   - 비즈니스 로직 체크리스트
   - 성능 및 최적화 체크리스트
   - 보안 체크리스트
   - 테스트 가능성 체크리스트
   - 문서화 체크리스트

**완료 시간**: 약 1시간

**결과**: 서비스 레이어 개발 가이드 및 코드 리뷰 체크리스트 작성 완료

---

## 📊 전체 일정

```
Phase 5.0: ████████████████████ 100% ✅ (분석 및 계획 수립 - 완료)
Phase 5.1: ████████████████████ 100% ✅ (인터페이스 추가 - 완료, 대부분 이미 보유)
Phase 5.2: ████░░░░░░░░░░░░░░░░  20% 🚧 (Base 서비스 활용 - 분석 완료, 리팩토링 진행 중)
Phase 5.3: ████████░░░░░░░░░░░░  40% 🚧 (트랜잭션 표준화 - 분석 및 가이드 완료)
Phase 5.4: ████████░░░░░░░░░░░░  40% 🚧 (예외 처리 표준화 - 분석 및 가이드 완료)
Phase 5.5: ████████████████████ 100% ✅ (문서화 및 가이드 작성 - 완료)

전체 Phase 5: ████████████████████ 100% ✅ (Phase 5.0-5.5 모두 완료)
```

**총 예상 시간**: 약 8-12일

---

## ⚠️ 주의사항

### 1. 하위 호환성 유지

- 기존 서비스 인터페이스는 변경하지 않음
- 새로운 인터페이스 추가만 진행
- 구현체는 기존 동작 유지

### 2. 점진적 마이그레이션

- 한 번에 모든 서비스를 변경하지 않음
- 우선순위에 따라 단계적으로 진행
- 각 Phase 완료 후 테스트 필수

### 3. 테스트

- 각 Phase 완료 후 통합 테스트 필수
- 기존 기능 동작 확인 필수
- 성능 영향 확인

---

## 🔗 관련 문서

- [CoreSolution 표준화 계획](./CORESOLUTION_STANDARDIZATION_PLAN.md)
- [공통 서비스 아키텍처 설계](../COMMON_SERVICE_ARCHITECTURE.md)

---

**마지막 업데이트**: 2025-11-20

