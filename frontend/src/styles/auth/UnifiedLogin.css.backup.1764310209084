/**
 * 통합 로그인 컴포넌트 스타일
 * Phase 3: 통합 로그인 시스템
 */

.unified-login {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  padding: var(--spacing-xl);
}

.unified-login__container {
  background-color: var(--bg-primary);
  border-radius: var(--border-radius-xl);
  box-shadow: var(--shadow-xl);
  padding: var(--spacing-xxl);
  max-width: 28rem;
  width: 100%;
}

.unified-login__header {
  text-align: center;
  margin-bottom: var(--spacing-xl);
}

.unified-login__title {
  font-size: var(--font-size-xxxl);
  font-weight: var(--font-weight-bold);
  color: var(--text-primary);
  margin-bottom: var(--spacing-sm);
}

.unified-login__subtitle {
  font-size: var(--font-size-md);
  color: var(--text-secondary);
}

.unified-login__form {
  margin-bottom: var(--spacing-xl);
}

.unified-login__field {
  margin-bottom: var(--spacing-lg);
}

.unified-login__label {
  display: block;
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
  margin-bottom: var(--spacing-sm);
}

.unified-login__input {
  width: 100%;
  padding: var(--spacing-md);
  border: 1px solid #d1d5db;
  border-radius: var(--border-radius-md);
  font-size: var(--font-size-md);
  transition: border-color var(--transition-fast);
}

.unified-login__input:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgba(0, 122, 255, 0.1);
}

.unified-login__password-wrapper {
  position: relative;
}

.unified-login__password-toggle {
  position: absolute;
  right: var(--spacing-md);
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: none;
  cursor: pointer;
  font-size: var(--font-size-lg);
  padding: var(--spacing-xs);
}

.unified-login__tooltip {
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--border-radius-md);
  margin-bottom: var(--spacing-md);
  font-size: var(--font-size-sm);
}

.unified-login__tooltip--error {
  background-color: #fee2e2;
  color: #991b1b;
  border: 1px solid #fecaca;
}

.unified-login__tooltip--success {
  background-color: #d1fae5;
  color: #065f46;
  border: 1px solid #6ee7b7;
}

.unified-login__tooltip--warning {
  background-color: #fef3c7;
  color: #92400e;
  border: 1px solid #fde68a;
}

.unified-login__submit-button {
  width: 100%;
  background-color: var(--color-primary);
  color: var(--text-inverse);
  padding: var(--spacing-md);
  border-radius: var(--border-radius-lg);
  font-weight: var(--font-weight-semibold);
  transition: background-color var(--transition-fast);
  border: none;
  cursor: pointer;
}

.unified-login__submit-button:hover:not(:disabled) {
  background-color: var(--color-primary-dark);
}

.unified-login__submit-button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.unified-login__social {
  margin-bottom: var(--spacing-xl);
}

.unified-login__social-divider {
  text-align: center;
  margin: var(--spacing-lg) 0;
  position: relative;
}

.unified-login__social-divider::before,
.unified-login__social-divider::after {
  content: '';
  position: absolute;
  top: 50%;
  width: 45%;
  height: 1px;
  background-color: #e5e7eb;
}

.unified-login__social-divider::before {
  left: 0;
}

.unified-login__social-divider::after {
  right: 0;
}

.unified-login__social-divider span {
  background-color: var(--bg-primary);
  padding: 0 var(--spacing-sm);
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

.unified-login__social-buttons {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.unified-login__social-button {
  width: 100%;
  padding: var(--spacing-md);
  border-radius: var(--border-radius-lg);
  font-weight: var(--font-weight-semibold);
  border: none;
  cursor: pointer;
  transition: opacity var(--transition-fast);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
}

.unified-login__social-button:hover {
  opacity: 0.9;
}

.unified-login__social-button--kakao {
  background-color: #FEE500;
  color: #000000;
}

.unified-login__social-button--naver {
  background-color: #03C75A;
  color: #ffffff;
}

.unified-login__social-button--google {
  background-color: #ffffff;
  color: #000000;
  border: 1px solid #d1d5db;
}

.unified-login__social-icon {
  font-size: var(--font-size-lg);
}

.unified-login__links {
  text-align: center;
  font-size: var(--font-size-sm);
}

.unified-login__link {
  color: var(--color-primary);
  text-decoration: none;
  transition: color var(--transition-fast);
}

.unified-login__link:hover {
  color: var(--color-primary-dark);
}

.unified-login__link-separator {
  margin: 0 var(--spacing-sm);
  color: var(--text-secondary);
}

