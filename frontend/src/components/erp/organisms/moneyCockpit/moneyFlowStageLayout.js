/**
 * MoneyFlowStage 차트 레이아웃 계약 상수 (1280 no-clip).
 * 플러그인 테스트·컴포넌트가 동일 SSOT를 참조한다.
 *
 * @author CoreSolution
 * @since 2026-08-31
 */

/** 평균 점선 라벨 fontSize — caption 12 */
export const AVG_LINE_LABEL_FONT_SIZE = 12;

/** chartArea 오른쪽 거터(px) — 평균 라벨이 plot 밖으로 나가도록 */
export const MONEY_FLOW_CHART_RIGHT_PADDING = 96;
