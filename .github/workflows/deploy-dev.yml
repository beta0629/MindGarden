name: 🧪 MindGarden 개발 서버 배포

on:
  push:
    branches: [ develop ]
    # develop 브랜치에 push하면 자동으로 개발 서버에 배포
    # main 브랜치는 운영 배포용이므로 개발 배포에서 제외
  workflow_dispatch:
    # 수동 실행 가능

jobs:
  deploy:
    name: 🏗️ 개발 서버 빌드 및 배포
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 코드 체크아웃
      uses: actions/checkout@v4
      
    - name: ☕ Java 17 설정
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: 📦 Node.js 18 설정
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
        
    - name: 🔧 Maven 캐시 설정
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
        
    - name: 🏗️ 백엔드 빌드
      run: mvn clean package -DskipTests
      
    - name: 📦 프론트엔드 의존성 설치
      run: |
        cd frontend
        npm ci
        
    - name: 🏗️ 프론트엔드 빌드
      run: |
        cd frontend
        npm run build:ci
        
    - name: 🔍 개발 서버 SSH 연결 테스트
      uses: appleboy/ssh-action@v1.1.0
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 30s
        script: |
          echo "SSH 연결 테스트 성공: $(date)"
          echo "서버 정보: $(uname -a)"
          
    - name: 🚀 개발 서버 배포 준비
      uses: appleboy/ssh-action@v1.1.0
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 120s
        command_timeout: 30m
        script: |
          # 배포 디렉토리로 이동
          cd /var/www/mindgarden-dev
          
          # 서비스 중지
          sudo systemctl stop mindgarden-dev.service || true
          
          # 백업 생성
          if [ -f app.jar ]; then
            cp app.jar app.jar.backup.$(date +%Y%m%d_%H%M%S)
          fi
          
          # 프론트엔드 백업
          if [ -d frontend ]; then
            tar -czf frontend.backup.$(date +%Y%m%d_%H%M%S).tar.gz frontend/
          fi
          
    - name: 📤 백엔드 파일 업로드 (개발 서버)
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 120s
        source: "target/consultation-management-system-1.0.0.jar"
        target: "/var/www/mindgarden-dev/"
        strip_components: 1
        overwrite: true
        
    - name: 📤 프론트엔드 파일 업로드 (개발 서버)
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 120s
        source: "frontend/build/*"
        target: "/var/www/html-dev/"
        strip_components: 2
        overwrite: true
        
    - name: 📤 Nginx 설정 파일 업로드 (개발 서버)
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 120s
        source: "config/nginx/dev.m-garden.co.kr.conf"
        target: "/tmp/"
        strip_components: 1
        overwrite: true
        
    - name: 📤 개발 서버 설정 파일 업로드
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 120s
        source: "src/main/resources/application-dev.yml"
        target: "/var/www/mindgarden-dev/"
        strip_components: 2
        overwrite: true
        
    - name: 📤 systemd 서비스 파일 업로드 (개발 서버)
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 120s
        source: "config/systemd/mindgarden-dev.service"
        target: "/tmp/"
        strip_components: 1
        overwrite: true
        
    - name: 🔄 개발 서버 서비스 재시작
      uses: appleboy/ssh-action@v1.1.0
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 60s
        command_timeout: 5m
        script: |
          cd /var/www/mindgarden-dev
          
          # JAR 파일명 변경 (운영과 동일)
          if [ -f consultation-management-system-1.0.0.jar ]; then
            mv consultation-management-system-1.0.0.jar app.jar
          fi
          
          # 권한 설정
          chmod +x app.jar
          
          # systemd 서비스 파일 설정 (없으면)
          if [ ! -f /etc/systemd/system/mindgarden-dev.service ]; then
            echo "📝 systemd 서비스 파일 설정 중..."
            if [ -f /tmp/mindgarden-dev.service ]; then
              sudo cp /tmp/mindgarden-dev.service /etc/systemd/system/mindgarden-dev.service
            elif [ -f /tmp/config/systemd/mindgarden-dev.service ]; then
              sudo cp /tmp/config/systemd/mindgarden-dev.service /etc/systemd/system/mindgarden-dev.service
              sudo systemctl daemon-reload
              sudo systemctl enable mindgarden-dev.service
              echo "✅ systemd 서비스 파일 설정 완료"
            else
              echo "⚠️ systemd 서비스 파일이 업로드되지 않음"
            fi
          fi
          
          # 환경 변수 파일 확인 (없으면 경고만)
          if [ ! -f /etc/mindgarden/dev.env ]; then
            echo "⚠️ 환경 변수 파일이 없습니다: /etc/mindgarden/dev.env"
            echo "⚠️ 수동으로 생성해야 합니다"
          else
            echo "✅ 환경 변수 파일 존재 확인"
          fi
          
          # 서비스 재시작
          echo "🔄 개발 서버 서비스 재시작..."
          sudo systemctl daemon-reload
          sudo systemctl stop mindgarden-dev.service || true
          sleep 5
          sudo systemctl start mindgarden-dev.service
          sleep 15
          
          # 서비스 상태 확인
          echo "🔍 서비스 상태 확인..."
          sudo systemctl status mindgarden-dev.service --no-pager | head -15
          
          # 서비스 활성화 상태 확인
          if sudo systemctl is-active --quiet mindgarden-dev.service; then
            echo "✅ 개발 서버 서비스 정상 시작됨"
          else
            echo "❌ 서비스 시작 실패 - 재시도..."
            sudo systemctl restart mindgarden-dev.service
            sleep 15
            sudo systemctl status mindgarden-dev.service --no-pager | head -15
          fi
          
          # Nginx 설정 업데이트 (설정 파일이 업로드된 경우)
          if [ -f /tmp/dev.m-garden.co.kr.conf ]; then
            echo "🌐 Nginx 설정 업데이트 중..."
            sudo cp /tmp/dev.m-garden.co.kr.conf /etc/nginx/sites-available/dev.m-garden.co.kr.conf
          elif [ -f /tmp/config/nginx/dev.m-garden.co.kr.conf ]; then
            echo "🌐 Nginx 설정 업데이트 중 (경로 수정)..."
            sudo cp /tmp/config/nginx/dev.m-garden.co.kr.conf /etc/nginx/sites-available/dev.m-garden.co.kr.conf
            sudo ln -sf /etc/nginx/sites-available/dev.m-garden.co.kr.conf /etc/nginx/sites-enabled/
            
            # 프론트엔드 디렉토리 권한 설정
            sudo chown -R www-data:www-data /var/www/html-dev
            sudo chmod -R 755 /var/www/html-dev
            
            # Nginx 설정 테스트
            if sudo nginx -t; then
              sudo systemctl reload nginx
              echo "✅ Nginx 설정 업데이트 완료"
            else
              echo "⚠️ Nginx 설정 오류 - 수동 확인 필요"
            fi
          fi
          
    - name: 🏥 개발 서버 헬스체크
      uses: appleboy/ssh-action@v1.1.0
      with:
        host: ${{ secrets.DEV_SERVER_HOST }}
        username: ${{ secrets.DEV_SERVER_USER }}
        key: ${{ secrets.DEV_SERVER_SSH_KEY }}
        port: 22
        timeout: 30s
        script: |
          echo "🏥 개발 서버 헬스체크 시작..."
          sleep 5
          
          # Health Check (최대 3회 재시도)
          for i in {1..3}; do
            if curl -f -s http://localhost:8080/actuator/health > /dev/null 2>&1; then
              echo "✅ 개발 서버 헬스체크 성공 (시도 $i)"
              curl -s http://localhost:8080/actuator/health | head -5
              exit 0
            else
              echo "⏳ 헬스체크 대기 중... (시도 $i/3)"
              sleep 10
            fi
          done
          
          echo "⚠️ 헬스체크 실패 - 서비스 로그 확인 필요"
          sudo journalctl -u mindgarden-dev.service --no-pager -n 50
          exit 1

