# 오늘 할 일 체크리스트

**날짜**: 2025-11-24
**작성 시간**: 오전
**상태**: 시작 전

---

## 🚨 긴급 이슈 (Blocker) - 최우선 해결 필요

### 1. 역할 템플릿 생성 문제 해결 ⭐⭐⭐
> **증상**: 온보딩 승인 시 `ApplyDefaultRoleTemplates` 프로시저가 실행되지 않아 역할이 생성되지 않음
> **영향**: 관리자 권한 할당 실패, 대시보드 생성 실패

- [x] **DB 프로시저 디버깅**
  - [x] `role_template_mappings` 테이블 데이터 확인 (CONSULTATION 업종 매핑)
  - [x] `ProcessOnboardingApproval` 프로시저 내 `ApplyDefaultRoleTemplates` 호출 부분 확인
  - [x] 프로시저 직접 실행 테스트 및 오류 메시지 확인
  - [x] `ApplyDefaultRoleTemplates` 프로시저 구현 및 적용
  - [x] `ProcessOnboardingApproval` 프로시저 수정 및 적용

### 2. Ops Portal 로그인 및 권한 문제 ⭐⭐⭐
> **증상**: 슈퍼 어드민 계정으로 Ops Portal 로그인 시 권한 문제 발생 가능성
> **영향**: 운영 관리 기능 사용 불가

- [ ] **Ops Portal 접근 권한 확인**
  - [ ] Ops 로그인 API 세션 생성 여부 확인
  - [ ] `checkMultiTenantUser` 및 권한 필터 동작 확인
  - [ ] 슈퍼 어드민의 `sys_admin` 권한 체크 로직 검증

### 3. 대시보드 상세 페이지 접근 불가 ⭐⭐⭐
> **증상**: 생성된 대시보드 진입 시 권한 오류 발생
> **영향**: 온보딩 완료 후 대시보드 확인 불가

- [ ] **대시보드 권한 로직 수정**
  - [ ] `TenantDashboardController` 접근 제어 로직 확인
  - [ ] 대시보드 소유권 vs 테넌트 관리자 권한 체크 로직 분리 확인

---

## 🚀 핵심 기능 개발 (Feature)

### 4. 온보딩 로그인 기능 추가 (Phase 1) ⭐⭐
> **목적**: 온보딩 중 이탈해도 진행 상황을 유지하고 이어서 진행

- [ ] **로그인 컴포넌트 구현**
  - [ ] `OnboardingLogin.tsx` 생성 (Trinity)
  - [ ] 온보딩 페이지 진입 시 로그인 상태 체크 로직 추가
- [ ] **진행 상태 저장 로직**
  - [ ] 로그인 시: DB에 단계(Step) 저장
  - [ ] 미로그인 시: localStorage에 단계 저장

### 5. 학원(Academy) 컴포넌트 개발 시작 ⭐⭐
> **목적**: 학원 업종 MVP 기능 구현

- [ ] **기반 구조 확인**
  - [ ] `AcademyDashboard.js` 및 하위 컴포넌트 구조 파악
  - [ ] 학원 관련 DB 스키마(`courses`, `classes`, `enrollments`) 확인
- [ ] **MVP 기능 명세 구체화**
  - [ ] 수강생 등록 및 반 배정 로직 설계

---

## 📝 검증 및 테스트

- [ ] **온보딩 전체 플로우 테스트**
  - [ ] 신청 -> 승인 -> 테넌트 생성 -> 역할 생성 -> 대시보드 생성 -> 로그인 -> 대시보드 진입

---

## 💡 메모
- **역할 템플릿 문제**가 해결되어야 이후 단계(대시보드, 권한) 테스트가 가능하므로 가장 먼저 처리해야 함.
- 온보딩 로그인 기능은 사용자 경험에 중요하므로 병렬로 진행 가능.

