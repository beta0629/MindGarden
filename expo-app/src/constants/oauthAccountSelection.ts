/**
 * OAuth 동일 전화 계정 선택 API — 웹 `oauthAccountSelectionConstants.js` 와 동일 경로
 *
 * @author MindGarden
 * @since 2026-05-14
 */
import { AUTH_API } from '@/api/endpoints';

export const OAUTH_ACCOUNT_SELECTION_PREVIEW_PATH = AUTH_API.OAUTH_ACCOUNT_SELECTION_PREVIEW;
export const OAUTH_ACCOUNT_SELECTION_COMPLETE_PATH = AUTH_API.OAUTH_ACCOUNT_SELECTION_COMPLETE;
