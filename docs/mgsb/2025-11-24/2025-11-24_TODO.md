# 2025-11-24 작업 계획 및 TODO 리스트

**작성일**: 2025-11-24  
**목적**: 온보딩 플로우 완성 및 역할 템플릿/대시보드 생성 문제 해결

---

## 🎯 주요 목표

1. **역할 템플릿 생성 문제 해결** ⭐⭐⭐
2. **역할 할당 문제 해결** ⭐⭐⭐
3. **Ops 로그인 및 권한 문제 해결** ⭐⭐⭐
4. **대시보드 상세 페이지 권한 문제 해결** ⭐⭐⭐
5. **대시보드 생성 문제 해결** ⭐⭐
6. **프론트엔드 로그인 테스트** ⭐⭐

---

## 📋 TODO 리스트

### 1. 역할 템플릿 생성 문제 해결 (우선순위: 높음)

#### 1.1 ProcessOnboardingApproval 프로시저 확인
- [ ] `ApplyDefaultRoleTemplates` 프로시저가 호출되는지 확인
- [ ] 프로시저 실행 순서 확인
- [ ] 프로시저 실행 로그 확인

#### 1.2 역할 템플릿 매핑 데이터 확인
- [ ] `role_template_mappings` 테이블에 CONSULTATION 업종 매핑 확인
- [ ] `role_templates` 테이블에 기본 템플릿 존재 확인
- [ ] 매핑 데이터가 없으면 생성

#### 1.3 ApplyDefaultRoleTemplates 프로시저 디버깅
- [ ] 프로시저 직접 실행 테스트
- [ ] 오류 메시지 확인
- [ ] 프로시저 로직 검증

**예상 시간**: 1-2시간

---

### 2. 역할 할당 문제 해결 (우선순위: 높음)

#### 2.1 역할 템플릿 생성 후 역할 할당
- [ ] `OnboardingServiceImpl.assignAdminRoleToUser()` 메서드 확인
- [ ] "관리자" 역할 이름 검색 로직 확인
- [ ] 역할 할당 로직 수정 (필요시)

#### 2.2 역할 할당 테스트
- [ ] 온보딩 승인 후 역할 할당 확인
- [ ] `user_role_assignments` 테이블에 데이터 생성 확인
- [ ] 역할 기반 권한 체크 테스트

**예상 시간**: 1시간

---

### 3. Ops 로그인 및 권한 문제 해결 (우선순위: 높음)

#### 3.1 Ops Portal 로그인 문제 확인
- [ ] Ops Portal 로그인 API 확인
- [ ] 세션 생성 확인
- [ ] 인증 필터 동작 확인
- [ ] 로그인 실패 원인 파악

#### 3.2 Ops Portal 권한 문제 확인
- [ ] Ops Portal 사용자 권한 확인
- [ ] 권한 체크 로직 확인
- [ ] 접근 제어 필터 확인
- [ ] 권한 부족 오류 메시지 확인

#### 3.3 권한 문제 해결
- [ ] 권한 체크 로직 수정 (필요시)
- [ ] 접근 제어 필터 수정 (필요시)
- [ ] Ops Portal 사용자 권한 설정 확인
- [ ] 권한 테스트

**예상 시간**: 1-2시간

---

### 4. 대시보드 상세 페이지 권한 문제 해결 (우선순위: 높음)

#### 4.1 대시보드 상세 페이지 접근 문제 확인
- [ ] 대시보드 상세 페이지 API 확인
- [ ] 권한 체크 로직 확인
- [ ] 접근 제어 필터 확인
- [ ] 권한 부족 오류 메시지 확인

#### 4.2 대시보드 권한 체크 로직 확인
- [ ] 대시보드 소유권 확인 로직
- [ ] 테넌트 접근 권한 확인 로직
- [ ] 역할 기반 접근 권한 확인 로직
- [ ] 권한 체크 실패 원인 파악

#### 4.3 권한 문제 해결
- [ ] 대시보드 권한 체크 로직 수정 (필요시)
- [ ] 접근 제어 필터 수정 (필요시)
- [ ] 권한 테스트
- [ ] 대시보드 상세 페이지 접근 확인

**예상 시간**: 1-2시간

---

### 5. 대시보드 생성 문제 해결 (우선순위: 중간)

#### 5.1 역할 템플릿 기반 대시보드 생성
- [ ] `TenantDashboardServiceImpl.createDefaultDashboards()` 메서드 확인
- [ ] 역할 템플릿 검색 로직 확인
- [ ] 대시보드 생성 로직 수정 (필요시)

#### 5.2 기본 대시보드 생성 테스트
- [ ] 온보딩 승인 후 대시보드 생성 확인
- [ ] `tenant_dashboards` 테이블에 데이터 생성 확인
- [ ] 기본 대시보드 3개 (CLIENT, CONSULTANT, ADMIN) 생성 확인

**예상 시간**: 1-2시간

---

### 6. 프론트엔드 로그인 테스트 (우선순위: 중간)

#### 6.1 테넌트 관리자 로그인 테스트
- [ ] `test1763912519@test.com` / `Test1234!@#` 로그인 테스트
- [ ] 세션 생성 확인
- [ ] 사용자 정보 조회 확인

#### 6.2 대시보드 표시 확인
- [ ] 대시보드 목록 조회 API 테스트
- [ ] 기본 대시보드 표시 확인
- [ ] 대시보드 라우팅 확인

#### 6.3 기능 접근 권한 확인
- [ ] 역할 기반 권한 체크 확인
- [ ] 메뉴 접근 권한 확인
- [ ] API 접근 권한 확인

**예상 시간**: 1-2시간

---

### 7. 추가 작업 (우선순위: 낮음)

#### 5.1 온보딩 플로우 문서화
- [ ] 온보딩 플로우 다이어그램 작성
- [ ] 프로시저 실행 순서 문서화
- [ ] 문제 해결 과정 문서화

#### 5.2 테스트 데이터 정리
- [ ] 테스트 테넌트 정리 (필요시)
- [ ] 테스트 사용자 정리 (필요시)
- [ ] 테스트 온보딩 요청 정리 (필요시)

**예상 시간**: 30분-1시간

---

## 🔍 디버깅 체크리스트

### ProcessOnboardingApproval 프로시저 확인
```sql
-- 프로시저 존재 확인
SHOW PROCEDURE STATUS WHERE Name = 'ProcessOnboardingApproval';

-- 프로시저 정의 확인
SHOW CREATE PROCEDURE ProcessOnboardingApproval;

-- ApplyDefaultRoleTemplates 호출 확인
-- (프로시저 내부 코드 확인)
```

### 역할 템플릿 매핑 확인
```sql
-- 역할 템플릿 매핑 확인
SELECT * FROM role_template_mappings 
WHERE business_type = 'CONSULTATION';

-- 역할 템플릿 확인
SELECT * FROM role_templates 
WHERE business_type = 'CONSULTATION' OR business_type IS NULL;
```

### 테넌트 역할 확인
```sql
-- 테넌트 역할 확인
SELECT * FROM tenant_roles 
WHERE tenant_id = 'test-tenant-1763912519';
```

### 역할 할당 확인
```sql
-- 역할 할당 확인
SELECT ura.*, u.email, r.role_name
FROM user_role_assignments ura
JOIN users u ON ura.user_id = u.id
JOIN tenant_roles r ON ura.tenant_role_id = r.tenant_role_id
WHERE u.tenant_id = 'test-tenant-1763912519';
```

### 대시보드 확인
```sql
-- 대시보드 확인
SELECT * FROM tenant_dashboards 
WHERE tenant_id = 'test-tenant-1763912519';
```

### Ops Portal 권한 확인
```sql
-- Ops Portal 사용자 확인
SELECT * FROM users 
WHERE email LIKE '%ops%' OR email LIKE '%admin%' OR role = 'BRANCH_SUPER_ADMIN';

-- Ops Portal 권한 확인
SELECT u.*, r.role_name, r.role_code
FROM users u
LEFT JOIN user_role_assignments ura ON u.id = ura.user_id
LEFT JOIN tenant_roles r ON ura.tenant_role_id = r.tenant_role_id
WHERE u.email = 'superadmin@mindgarden.com';
```

### 대시보드 상세 페이지 권한 확인
```sql
-- 대시보드 소유권 확인
SELECT 
    td.id,
    td.tenant_id,
    td.name,
    td.created_by,
    u.email as creator_email
FROM tenant_dashboards td
LEFT JOIN users u ON td.created_by = u.email
WHERE td.id = ?;

-- 사용자 대시보드 접근 권한 확인
SELECT 
    u.id as user_id,
    u.email,
    u.tenant_id,
    td.id as dashboard_id,
    td.name as dashboard_name
FROM users u
JOIN tenant_dashboards td ON u.tenant_id = td.tenant_id
WHERE u.id = ? AND td.id = ?;
```

---

## 📊 예상 작업 시간

| 작업 | 예상 시간 | 우선순위 |
|------|----------|---------|
| 역할 템플릿 생성 문제 해결 | 1-2시간 | 높음 |
| 역할 할당 문제 해결 | 1시간 | 높음 |
| Ops 로그인 및 권한 문제 해결 | 1-2시간 | 높음 |
| 대시보드 상세 페이지 권한 문제 해결 | 1-2시간 | 높음 |
| 대시보드 생성 문제 해결 | 1-2시간 | 중간 |
| 프론트엔드 로그인 테스트 | 1-2시간 | 중간 |
| 추가 작업 | 30분-1시간 | 낮음 |
| **총 예상 시간** | **7-12시간** | |

---

## 🎯 성공 기준

1. ✅ 온보딩 승인 후 역할 템플릿 자동 생성
2. ✅ 관리자 역할 자동 할당
3. ✅ Ops Portal 로그인 성공
4. ✅ Ops Portal 권한 체크 통과
5. ✅ 대시보드 상세 페이지 접근 성공
6. ✅ 기본 대시보드 3개 자동 생성
7. ✅ 프론트엔드 로그인 성공
8. ✅ 대시보드 표시 확인
9. ✅ 기능 접근 권한 확인

---

## 📝 참고 자료

- [2025-11-23 작업 요약](./2025-11-23_WORK_SUMMARY.md)
- [온보딩 프로세스 문서](../ONBOARDING_ADMIN_ACCOUNT_PROCESS.md)
- [대시보드 관리 시스템](../TENANT_DASHBOARD_MANAGEMENT_SYSTEM.md)
- [ProcessOnboardingApproval 프로시저 코드](../CORE_SOLUTION_PLSQL_ARCHITECTURE.md)

---

**작성자**: 개발팀  
**상태**: 준비 완료  
**시작 예정 시간**: 2025-11-24 오전

