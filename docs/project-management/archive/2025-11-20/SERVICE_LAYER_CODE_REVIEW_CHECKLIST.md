# 서비스 레이어 코드 리뷰 체크리스트

**작성일**: 2025-11-20  
**버전**: 1.0.0  
**상태**: 완료

---

## 📋 개요

서비스 레이어 코드 리뷰 시 확인해야 할 항목들을 체크리스트로 정리한 문서입니다.

---

## ✅ 서비스 인터페이스 체크리스트

### 구조 및 네이밍

- [ ] 인터페이스와 구현체 분리되어 있는가?
- [ ] 인터페이스 이름이 `*Service` 형식인가?
- [ ] 패키지가 `service` 패키지에 있는가?
- [ ] JavaDoc이 작성되어 있는가?

### 메서드 정의

- [ ] 모든 public 메서드에 JavaDoc이 작성되어 있는가?
- [ ] 메서드가 섹션으로 그룹화되어 있는가? (예: `// ==================== 기본 조회 메서드 ====================`)
- [ ] 예외가 `@throws` 태그로 명시되어 있는가?
- [ ] 메서드 시그니처가 명확한가?

### BaseService 활용

- [ ] BaseService를 확장할 수 있는가?
- [ ] BaseService를 확장하는 경우 적절히 사용하고 있는가?

---

## ✅ 서비스 구현체 체크리스트

### 구조 및 네이밍

- [ ] 구현체 이름이 `*ServiceImpl` 형식인가?
- [ ] 패키지가 `service.impl` 패키지에 있는가?
- [ ] 인터페이스를 구현하고 있는가? (`implements UserService`)
- [ ] JavaDoc이 작성되어 있는가?

### 어노테이션

- [ ] `@Slf4j` 어노테이션이 있는가?
- [ ] `@Service` 어노테이션이 있는가?
- [ ] `@Transactional` 어노테이션이 있는가? (클래스 레벨)
- [ ] `@RequiredArgsConstructor` 어노테이션이 있는가?

### 의존성 주입

- [ ] 생성자 주입을 사용하고 있는가?
- [ ] `@Autowired` 필드 주입을 사용하지 않는가?
- [ ] final 필드를 사용하고 있는가?

### 트랜잭션 관리

- [ ] 클래스 레벨 `@Transactional`이 있는가?
- [ ] 읽기 전용 메서드에 `@Transactional(readOnly = true)`가 있는가?
- [ ] 특수한 롤백 정책이 필요한 경우 명시되어 있는가?

### 로깅

- [ ] 적절한 로그 레벨을 사용하고 있는가?
  - `log.debug()`: 디버그 정보
  - `log.info()`: 비즈니스 로직 시작/완료
  - `log.warn()`: 경고 상황
  - `log.error()`: 오류 상황
- [ ] 민감한 정보(비밀번호, 개인정보)를 로그에 포함하지 않는가?
- [ ] 구조화된 로깅을 사용하는가? (키-값 쌍)

### 예외 처리

- [ ] 비즈니스 예외는 커스텀 예외를 사용하는가?
- [ ] `EntityNotFoundException`을 사용하는가? (엔티티를 찾을 수 없을 때)
- [ ] `ValidationException`을 사용하는가? (검증 실패 시)
- [ ] 예외 메시지가 사용자 친화적인가? (한국어)
- [ ] 예외 메시지에 민감한 정보가 포함되지 않는가?
- [ ] `RuntimeException`을 직접 throw하지 않는가?

### Base 서비스 활용

- [ ] Base 서비스를 활용할 수 있는가?
- [ ] BaseTenantEntityServiceImpl을 상속할 수 있는가?
- [ ] 공통 CRUD 로직이 중복되어 있지 않은가?

---

## ✅ 비즈니스 로직 체크리스트

### 검증

- [ ] 입력 데이터 검증이 적절히 수행되는가?
- [ ] 검증 실패 시 `ValidationException`을 throw하는가?
- [ ] 검증 메시지가 명확한가?

### 엔티티 조회

- [ ] 엔티티를 찾을 수 없을 때 `EntityNotFoundException`을 throw하는가?
- [ ] `Optional`을 적절히 사용하는가?
- [ ] `orElseThrow()`를 사용하는가?

### 데이터 변환

- [ ] DTO → Entity 변환이 적절히 수행되는가?
- [ ] Entity → DTO 변환이 적절히 수행되는가?
- [ ] 변환 로직이 명확한가?

### 트랜잭션 범위

- [ ] 트랜잭션이 필요한 작업이 트랜잭션 내에서 수행되는가?
- [ ] 트랜잭션 범위가 적절한가? (너무 넓지 않거나 좁지 않은가?)

---

## ✅ 성능 및 최적화 체크리스트

### 쿼리 최적화

- [ ] N+1 문제가 없는가?
- [ ] 필요한 데이터만 조회하는가?
- [ ] 페이징을 적절히 사용하는가?

### 캐싱

- [ ] 캐싱이 필요한 데이터에 캐싱을 적용하는가?
- [ ] 캐시 무효화가 적절히 수행되는가?

### 읽기 전용 트랜잭션

- [ ] 읽기 전용 메서드에 `@Transactional(readOnly = true)`가 있는가?
- [ ] 읽기 전용 서비스는 클래스 레벨 `@Transactional(readOnly = true)`를 사용하는가?

---

## ✅ 보안 체크리스트

### 데이터 접근 제어

- [ ] 테넌트 접근 제어가 적절히 수행되는가?
- [ ] 권한 검증이 적절히 수행되는가?
- [ ] 사용자가 자신의 데이터만 접근할 수 있는가?

### 민감한 정보 처리

- [ ] 비밀번호가 암호화되어 저장되는가?
- [ ] 개인정보가 암호화되어 저장되는가?
- [ ] 로그에 민감한 정보가 포함되지 않는가?

---

## ✅ 테스트 가능성 체크리스트

### 의존성 주입

- [ ] 생성자 주입을 사용하여 테스트 가능한가?
- [ ] 인터페이스를 사용하여 모킹이 가능한가?

### 메서드 분리

- [ ] 메서드가 적절히 분리되어 있는가?
- [ ] 단위 테스트가 가능한 구조인가?

---

## ✅ 문서화 체크리스트

### JavaDoc

- [ ] 클래스에 JavaDoc이 작성되어 있는가?
- [ ] 모든 public 메서드에 JavaDoc이 작성되어 있는가?
- [ ] 파라미터에 `@param` 태그가 있는가?
- [ ] 반환값에 `@return` 태그가 있는가?
- [ ] 예외에 `@throws` 태그가 있는가?

### 코드 주석

- [ ] 복잡한 비즈니스 로직에 주석이 있는가?
- [ ] 주석이 코드와 일치하는가?
- [ ] 불필요한 주석이 없는가?

---

## 📊 체크리스트 사용 가이드

### 코드 리뷰 시

1. **서비스 인터페이스**: 인터페이스 체크리스트 확인
2. **서비스 구현체**: 구현체 체크리스트 확인
3. **비즈니스 로직**: 비즈니스 로직 체크리스트 확인
4. **성능 및 최적화**: 성능 체크리스트 확인
5. **보안**: 보안 체크리스트 확인
6. **테스트 가능성**: 테스트 가능성 체크리스트 확인
7. **문서화**: 문서화 체크리스트 확인

### 우선순위

- **필수 (P0)**: 구조, 어노테이션, 예외 처리, 보안
- **권장 (P1)**: Base 서비스 활용, 트랜잭션 관리, 로깅
- **선택 (P2)**: 성능 최적화, 문서화

---

## 🔗 관련 문서

- [서비스 레이어 개발 가이드](./SERVICE_LAYER_GUIDE.md)
- [Base 서비스 활용 현황 분석](./BASE_SERVICE_USAGE_ANALYSIS.md)
- [트랜잭션 표준화 가이드](./TRANSACTION_STANDARDIZATION_GUIDE.md)
- [예외 처리 표준화 가이드](./EXCEPTION_HANDLING_STANDARDIZATION_GUIDE.md)

---

**마지막 업데이트**: 2025-11-20

