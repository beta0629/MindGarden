/**
 * 위젯 카드 래퍼 스타일
 * 동적 카드 레이아웃을 위한 스타일 정의
 */

/* 기본 카드 래퍼 */
.widget-card-wrapper {
  width: 100%;
  height: 100%;
  transition: all 0.3s ease;
  box-sizing: border-box;
}

/* MindGarden v2 카드 스타일 */
.mg-v2-card {
  background: white;
  border: 1px solid var(--mg-gray-300);
}

/* 글래스모피즘 카드 */
.mg-glass-card {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.mg-glass-effect {
  backdrop-filter: blur(10px);
  background: rgba(255, 255, 255, 0.8);
}

/* 평면 스타일 카드 */
.mg-flat-card {
  background: var(--mg-gray-100);
  border: none;
  box-shadow: none;
}

/* 테두리 강조 카드 */
.mg-bordered-card {
  border: 2px solid var(--mg-primary-500);
  background: white;
}

.mg-card-bordered {
  border: 1px solid #dee2e6;
}

/* 미니멀 스타일 카드 */
.mg-minimal-card {
  background: transparent;
  border: none;
  box-shadow: none;
  padding: 0;
}

/* 카드 Variant */
.mg-card-elevated {
  box-shadow: 0 2px 4px var(--mg-shadow-light);
}

.mg-card-outlined {
  border: 1px solid #dee2e6;
  box-shadow: none;
}

.mg-card-filled {
  background: var(--mg-gray-100);
  border: none;
}

.mg-card-text {
  background: transparent;
  border: none;
  box-shadow: none;
}

/* 패딩 크기 */
.mg-padding-none {
  padding: 0;
}

.mg-padding-sm {
  padding: 0.5rem;
}

.mg-padding-md {
  padding: 1rem;
}

.mg-padding-lg {
  padding: 1.5rem;
}

.mg-padding-xl {
  padding: 2rem;
}

/* 보더 반경 */
.mg-radius-none {
  border-radius: 0;
}

.mg-radius-sm {
  border-radius: 4px;
}

.mg-radius-md {
  border-radius: 8px;
}

.mg-radius-lg {
  border-radius: 12px;
}

.mg-radius-full {
  border-radius: 9999px;
}

/* 그림자 크기 */
.mg-shadow-none {
  box-shadow: none;
}

.mg-shadow-sm {
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
}

.mg-shadow-md {
  box-shadow: 0 2px 4px var(--mg-shadow-light);
}

.mg-shadow-lg {
  box-shadow: 0 4px 8px var(--mg-shadow-medium);
}

.mg-shadow-xl {
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
}

/* 호버 효과 - 표준화 원칙 준수: CSS 변수 사용, 일관된 호버 효과 */
.mg-card-hover {
  cursor: pointer;
  transition: var(--mg-card-transition, all 0.3s cubic-bezier(0.4, 0, 0.2, 1));
}

.mg-card-hover:hover {
  transform: var(--mg-card-hover-transform, translateY(-2px));
  box-shadow: var(--mg-card-hover-shadow, 0 4px 12px rgba(0, 0, 0, 0.15));
}

/* 동적 스타일 (CSS 변수 사용) */
.widget-card-wrapper {
  background-color: var(--card-bg-color, inherit);
  border-color: var(--card-border-color, inherit);
  border-width: var(--card-border-width, 0);
  border-style: var(--card-border-style, none);
}

/* 반응형 디자인 */
@media (max-width: calc(var(--breakpoint-tablet, 768px) - 1px)) {
  .mg-padding-lg,
  .mg-padding-xl {
    padding: 1rem;
  }
}

